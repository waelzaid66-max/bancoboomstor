# إلى المدير Codex — مصدر الحقيقة بعد الهجرة · بالتواريخ والأدلة

**من:** الوكيل المساعد (`session_011AM6FmwA4L1EfrkHmr7J28`)
**التاريخ:** 2026-08-06 · **`main` = `d3f8df1`**

> كل سطر تحته أمر. **مفيش حرف اتكتب من الذاكرة.**

---

# ١) الخط الزمني — بالتاريخ والساعة

| التاريخ والساعة | الحدث | الدليل |
|---|---|---|
| **2026-07-08** | وسم `v1.0.0-rc.1` على **ريبوين**: `-BANCO-CA-OOM-` (الأساسي) + `aws-virgen` (مرآة AWS بنشر بالأوسمة) | `RELEASE_NOTES.md` سطر 3-4 |
| **2026-07-30** | `DEPLOYMENT_SOURCE_OF_TRUTH.md` اتولّد — وسمّى `banco-with-wael` **«ONLY deploy SoT repository»** | ترويسة المستند |
| **2026-08-01 04:23** | 🔴 **الهجرة** — `89d28d3` «initialise bancoboomstor as the single production source of truth» · **2536 ملف · 402,921 سطر** في كوميت واحد | `git show --stat 89d28d3` |
| **2026-08-01 11:04** | أول شغل حقيقي بعد الهجرة — نظام الميجريشن | `git log --reverse` |
| **2026-08-04 14:04** | آخر كوميت على `main` | `git log -1 origin/main` |
| **2026-08-06 15:53** | آخر نشاط — **على فرع مش نازل** | `git for-each-ref` |

**الشغل بعد الهجرة: 88 كوميت.**
```bash
git rev-list --count 89d28d3..origin/main    # 88
```

---

# ٢) ✅ الحكم — مصدر الحقيقة بعد الهجرة

## **`waelzaid66-max/bancoboomstor` — وده اللي إنت شغال فيه.**

**تأكيد المالك مباشرة**، ومدعوم بتلات أدلة من التاريخ نفسه:

1. **كوميت الهجرة بيسمّي نفسه كده حرفيًا** (2026-08-01 04:23):
   «initialise bancoboomstor as **the single production source of truth**»
2. **كل التطوير من 08-01 لحد النهاردة هنا** — 88 كوميت، أربع جلسات وكلاء
3. **الريبوهات القديمة مالهاش أي نشاط في السلسلة دي** — ولا كوميت واحد
   على الخرايط مثلًا اتعمل هنا؛ كلها جت جوه سنابشوت الهجرة

## 🔴 وتلات مستندات **بتقول غير كده** — لسه على `main` دلوقتي

| المستند | بيقول | الحالة |
|---|---|---|
| `docs/DEPLOYMENT_SOURCE_OF_TRUTH.md` (07-30) | `banco-with-wael` = **«ONLY deploy SoT»** | 🔴 **قبل الهجرة بيومين — بايت** |
| `README.md` | `-BANCO-CA-OOM-` أساسي + `aws-virgen` | 🔴 **بايت** |
| `DUAL_REPO_STATUS.md` | نفس الاتنين | 🔴 **بايت** |

**التبعة:** أي وكيل جديد يقرا `DEPLOYMENT_SOURCE_OF_TRUTH.md` — وهو المستند
اللي اسمه حرفيًا «مصدر الحقيقة» — **هينشر ريبو تاني**.

**أول كوميت ليك يصحّح التلاتة. مش تنظيف — ده حاجز أمان.**

---

# ٣) 🔴 الأوسمة — الهجرة نقلت الكود ومنقلتش سلسلة الإصدارات

**دي النقطة اللي سألت عنها، ودي الإجابة الدقيقة:**

```bash
git tag -l | wc -l                                    # 0
git ls-remote --tags origin | grep -v '\^{}' | wc -l  # 0
node -e "console.log(require('./package.json').version)"  # 0.0.0
```

## **`bancoboomstor` فيه صفر أوسمة. صفر.**

بينما:
- **`v1.0.0-rc.1`** موجود على **`-BANCO-CA-OOM-`** و**`aws-virgen`** من **2026-07-08**
- والإصدار في `package.json` الجذر لسه **`0.0.0`**

## والنتيجة العملية — خطيرة

```yaml
# .github/workflows/deploy.yml
on:
  push:
    tags: ["v*.*.*"]        # ← النشر للإنتاج بيولّع بالأوسمة بس
  workflow_dispatch:
    inputs: { ref: { description: "Tag or SHA to deploy", required: true } }
```

**`deploy.yml` موجود في الريبو ده ومستحيل يولّع تلقائيًا — لأن مفيش أوسمة
أصلًا.** والوسم الوحيد المعروف (`v1.0.0-rc.1`) عايش في الريبوهات القديمة.

**يعني بالنص:**
> سلسلة الإصدارات اتقطعت عند الهجرة. الكود اتنقل، **الهوية الإصدارية لأ**.
> مصدر الحقيقة الحالي **مالوش ولا وسم واحد**، ومسار نشره للإنتاج **معطّل
> فعليًا**.

## توصيتي — قرار المالك مش قرارنا

| الخيار | المعنى |
|---|---|
| أ | وسم `v1.0.0` (أو `rc.2`) على `bancoboomstor` — **يستأنف السلسلة ويفعّل `deploy.yml`** |
| ب | يفضل النشر عبر `workflow_dispatch` بـSHA — شغال، بس **بلا هوية إصدار** |

**لا تعمل وسم من غير أمر مكتوب من المالك.** الوسم بيولّع نشر إنتاج.

---

# ٤) الحالة دلوقتي — مختصرة

## 🔴 عاجل
**باج أيقونة الإرسال اللي المالك شافه بعينه لسه حي على `main`:**
```bash
git show origin/main:artifacts/banco-mobile/components/icons.tsx | grep 'const FILLED'
# const FILLED = new Set(["heart", "star"])   ← ناقصة "send"
```
الإصلاح مُثبت على `claude/are-you-here-84u6fs` (`9f04383`, **08-04 17:07**) — **مش نازل**.

## الشغل المحبوس
**6 فروع · يومين.** أثمنهم `headers-dynamic-polish` (B2, 17 كوميت) وفيه
**تقسيم هيدر العقارات و B-CORE** + **شغل الخرايط** + إصلاح `REPLACE_`.

**ترتيب الدمج مختبَر** وحلول التعارض السبعة مكتوبة في:
`audit/handoff/TO-MANAGER-AGENT-INTEGRATION-AUDIT-AR.md`

النتيجة اللي وصلتلها: **typecheck نضيف · 28 ملف حارس بصفر فشل**.

## أخطر عيب تقني
**`sectionAccentAlpha` معرّفة مرتين** — git بيدمجها **نظيفة** والبناء يقع
(`TS2323` + `TS2393`). `main` فيها صفر؛ وكيلان ضافوها في مكانين مختلفين من
نفس الملف. **ولا فرع غلطان لوحده.**

## المرجع الكامل
`audit/handoff/TO-MANAGER-FULL-BRIEF-AR.md` — الأيقونات وأندرويد · تحذير
الأخطاء المتكررة بتواريخها · حالة النشر · معايير القبول.

---

# ٥) خمس نقاط أرجوك تثبّتها في ذهنك

1. **الريبو ده هو النسخة الحقيقية الكاملة للنشر.** تلات مستندات جواه بتقول
   غير كده وهي بايتة — من قبل الهجرة.
2. **مفيش أوسمة هنا.** مسار النشر بالأوسمة معطّل. القرار للمالك.
3. **الـCI مابيولّعش لوحده** على كوميتات الوكلاء — لا `push` ولا
   `pull_request`. **`workflow_dispatch` يدوي هو الطريق الوحيد المثبت.**
4. **اختبر الحالة المدموجة مش الفرع.** `sectionAccentAlpha` هي الدليل.
5. **مفيش حكم بصري ممكن من البيئات دي** — Clerk بيرفض الأصل. القياس
   الهندسي موثوق، الحكم البصري لأ. أي تسليم بيدّعي «اتحققت بصريًا» من هنا
   — **ارفضه**.

---

**تحت أمرك.** كل رقم فوق قابل لإعادة الإنتاج بالأمر المكتوب جنبه.

— الوكيل المساعد · `session_011AM6FmwA4L1EfrkHmr7J28`
