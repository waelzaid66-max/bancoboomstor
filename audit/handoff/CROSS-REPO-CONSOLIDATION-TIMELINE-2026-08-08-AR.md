# أوديت التوحيد عبر الريبوهات + التسلسل التاريخي — إلى المدير Codex

**من:** وكيل الجودة (Claude) · **التاريخ:** 2026-08-08
**القانوني:** `bancoboomstor` @ `36766cf` · **الطريقة:** clone + مقارنة محتوى (لا نسب git بين الريبوهات — اتقطع في الهجرة)

> السؤال: النسخ الأصلية الموسومة «مصدر الحقيقة» (`banco-with-wael`, `bancoo`) — **إيه اللي منها مـ‑وصلش** للمجمّعة الكبيرة؟ الجواب بالأدلة تحت.

---

## ١) التسلسل التاريخي الكامل (UTC/local، بالثواني)

| التوقيت | الحدث | الدليل (SHA) |
|---|---|---|
| **2026-07-21 14:34:13** | لقطة handoff واحدة `321af02` «source 93f2c7e» → اتفرّع منها **`banco-with-wael`** و**`bancoo`** (نفس الجذر) | `git log --reverse` للاتنين |
| **2026-07-28 23:36:35** | `bancoo` ينتهي — `c1c5c00` (facebook-oauth, PR#8). **67 كوميت. بلا أوسمة** | `git log -1` |
| **2026-07-30** | `docs/DEPLOYMENT_SOURCE_OF_TRUTH.md` يعلن `banco-with-wael` = «ONLY deploy SoT» | ترويسة المستند |
| **2026-07-31 23:25:40** | `banco-with-wael` ينتهي — `c67eb4b` (EAS build+publish). **398 كوميت. وسم `w.4.1`** | `git log -1` + `git tag` |
| **2026-08-01 04:23:20** | ⚡ **الهجرة** — `bancoboomstor` يبدأ بكوميت squash واحد `89d28d3` (2536 ملف) «single production source of truth» | `git show -s 89d28d3` |
| **2026-08-01 → 08-08** | 88+ كوميت توحيد/تطوير على المجمّعة | `git rev-list --count 89d28d3..main` |
| **2026-08-08 19:00** | هذا الأوديت + إصلاحاتي على `claude/qa-audit-fixes` | PR #8 |

**قرابة النسب:** صفر كوميت مشترك بين `bancoboomstor` وأي من `banco-with-wael`/`bancoo`.
```bash
git log --format=%H | grep -Ff <(git --git-dir=../audit-banco-with-wael/.git log --format=%H) | wc -l   # 0
```
→ الهجرة **نسخ محتوى، مش دمج git**. النتيجة: **التاريخ اتقطع + الأوسمة ضاعت** (بند مسار النشر في تقرير QA الآخر).

---

## ٢) ✅ جرد «إيه اللي مـ‑وصلش» — بدون ضوضاء

مقارنة شجرة الملفات (باستثناء `node_modules`/lock/dist/DB dumps):

| من | ملفات غايبة عن المجمّعة | الحكم |
|----|:---:|-------|
| `banco-with-wael` | **2 فقط** | `.github/workflows/sync-aws-virgen.yml` + `sync-bancooom.yml` — أتمتة مزامنة **لريبوهات قديمة**، مقصود ما تتنقلش. **لا شيء جوهري ضاع** |
| `bancoo` | **2 فقط** (نفسهم) | نفس الحكم |

**آخر ميزة في كل أصل — وصلت؟**
```bash
grep -q "app-bundle\|autoIncrement" bancoboomstor/artifacts/banco-mobile/eas.json   # ✅ EAS (with-wael)
grep -rq "oauth_facebook" bancoboomstor/artifacts/banco-mobile                       # ✅ facebook (bancoo)
```

**73 ملف كود مختلف** بين `banco-with-wael` و`bancoboomstor` = **المجمّعة أحدث** (88+ كوميت بعد اللقطة اللي أصلًا ضمّت محتوى with-wael)، **مش شغل مفقود**.

### الحكم: توحيد المحتوى **مكتمل**. النسخ الأصلية اندمجت فعليًا في المجمّعة على مستوى الملفات؛ الوحيد اللي «اتسايب» = أتمتة مزامنة ريبوهات ميتة (صح إنها ما تتنقلش).

---

## ٣) واقع النسخة الحالية (main @ 36766cf) — بلا تضليل

| البند | الواقع | الإجراء |
|-------|--------|---------|
| 🔴 بناء الموبايل | `sectionAccentAlpha` مكررة → TS2323/2393، `tsc` يقع | **متصلح على PR #8** |
| 🔴 test:icons | 4 أيقونات غير مسجّلة | **متصلح على PR #8** |
| 🔴 مسار النشر | صفر أوسمة + `deploy.yml` بالأوسمة → نشر تلقائي معطّل | قرار مالك (وسم `v1.0.0`) |
| 🔴 مستند SoT | `DEPLOYMENT_SOURCE_OF_TRUTH.md` لسه بيوجّه لـ `banco-with-wael` | يُوحَّد على المجمّعة |
| 🟠 شغل متروك | `headers-dynamic-polish`: تحصين well-known + بوابات | يُدمج بعد مراجعة |
| ✅ أسرار / توحيد | صفر مفاتيح حية · توحيد الموبايل مكتمل | — |

---

## ٤) الملفات القديمة/المكسورة المرشّحة للحذف — **بعد موافقة المدير فقط**

لا أحذف شيئًا الآن. مرشّحون للمراجعة (كلهم يحتاجون تأكيد Codex إنهم غير ضروريين):
1. `docs/DEPLOYMENT_SOURCE_OF_TRUTH.md` + `DUAL_REPO_STATUS.md` — **بايتة وسامّة** (توجّه لريبو غلط). التوصية: تصحيح لا حذف.
2. النسخة القديمة من `sectionAccentAlpha` — **حُذفت فعلًا** في PR #8 (مكسورة قطعًا).
3. `sync-aws-virgen.yml` / `sync-bancooom.yml` (لو موجودة في أي فرع) — تخص ريبوهات ميتة.

> أي حذف تاني يمرّ ببوابة: (أ) موافقة Codex، (ب) تأكيد عدم وجود مستدعٍ حي، (ج) عدم كسر بناء.

---

## ٥) للمدير — طلب تنسيق مباشر

1. **راجع PR #8** — 3 إصلاحات (أهمها كسر البناء). دمجها يفكّ typecheck + الاختبارات.
2. **قرار:** دمج `headers-dynamic-polish` (تحصين well-known)؟
3. **تصحيح مستند SoT** لتفادي نشر ريبو غلط.
4. **قرار المالك:** وسم `v1.0.0` لتفعيل النشر.

**أنا منضمّ للشغل الآن، ومستعد أنفّذ أي بند فور موافقتك.**

— وكيل الجودة · متزامن مع `bancoboomstor`
