# اللي فهمناه صح ومنفّذناهوش — والسبب الحقيقي

**من:** الوكيل المساعد (`session_011AM6FmwA4L1EfrkHmr7J28`)
**التاريخ:** 2026-08-06 · **`main` = `d3f8df1`**

> بلا تجميل. السبب الحقيقي مكتوب حتى لو كان بيدين وكيل.

---

# ١) اللي اتفهم صح واتنفّذ **على فرع** ومنزلش — والسبب

## أ · الخرايط — **429 سطر · صفر تعارض · محبوسة**

```bash
git diff --stat origin/main...origin/claude/headers-dynamic-polish -- \
  '.../search/maps/' '.../search/mapHtml.ts' '.../search/SearchResultsMap*' \
  '.../search/MapOverlayChrome.tsx'
```
```
MapOverlayChrome.tsx      46 +++
SearchResultsMap.tsx     115 +++++
SearchResultsMap.web.tsx  22 +
mapHtml.ts               271 +++++++++++
4 files changed, 429 insertions(+), 25 deletions(-)
```

**تعارضات ملفات الخرايط لو اتدمجت: صفر.**

| البند | الحالة |
|---|---|
| رفع أدوات الخريطة من تحت الشريط اللي كان بيدفنها | ✅ متعمل |
| رسم مساحة بحث (MAP-08b — كان «مؤجّل بقرار منتج») | ✅ متعمل |
| علامة القابل للحجز بدل إيموجي مستعار | ✅ متعمل |

### 🔴 السبب الحقيقي للانحباس

**مش الخرايط.** الفرع اللي شايلها فيه كمان تعديلات على `app/import/` و
`package.json` و`PresenceLabel.render.test.tsx` — **ودي اللي بتتعارض**.

**يعني: 429 سطر خرايط نضيفة محبوسة بسبب ملفات مالهاش علاقة بيها.**

**والسبب الجذري:** الوكيل حط الخرايط والاستيراد والاختبارات **على فرع
واحد**. لو الخرايط كانت لوحدها كانت نزلت من يومين.

**درس:** فرع واحد لكل نطاق. الفرع المختلط بيورّث تعارض نطاق لنطاق تاني.

## ب · تقسيم الهيدرز — اتعمل ومنزلش

`1bfa485` — «split and collapse Property and B-CORE, on the Facilities model».
**ده اللي المالك بيسأل عنه من أول الجلسة.** على نفس الفرع المحبوس.

## ج · إصلاح أيقونة الإرسال — اتعمل ومُثبت ومنزلش

`9f04383` (08-04 17:07) على `are-you-here-84u6fs` — **الفرع ده صفر تعارض**.
**مفيش أي سبب تقني لعدم نزوله.** السبب الوحيد: محدش دمجه.

---

# ٢) اللي اتفهم صح و**ما اتنفّذش أصلًا** — والسبب

| البند | فُهم في | ليه ما اتعملش |
|---|---|---|
| **الحضور بصريًا** | 08-03 | **مانع بيئي حقيقي** — Clerk بيرفض أصل البيئات السحابية. مفيش وكيل يقدر يسجّل دخول |
| **MSG-08b** حظر متبادل | 07-31 | محتاج تعديل schema — **قرار منتج مش متاخد** |
| **MSG-13** كتم لكل محادثة | 07-31 | نفس السبب |
| **MSG-14c** تسجيل صوتي | 07-31 | الـAPI بيقبل صوت · الواجهة لأ — **شغل مش متبدئ** |
| **تواريخ الحجز في العقد** | 08-03 (جردي) | **اتوزّع على B1 ومحدش نفّذه** |
| **`ensureSchemaPatches` سلطتين** | 08-03 (جردي) | ✅ **اتعمل** — اختبار على فرع TCP |
| **`REPLACE_` في well-known** | 08-03 (جردي) | ✅ **اتعمل** — `5e9437e` محبوس |

**الخلاصة:** من 7 بنود، **2 مانع تقني حقيقي، 2 قرار منتج، 2 اتعملوا ومحبوسين،
1 اتوزّع ومحدش مسكه.**

---

# ٣) الباك إند — الجرد

| المقياس | العدد |
|---|---|
| خدمات (`services/*.ts`) | **52** |
| متحكمات | **33** |
| مسارات `v1` | **31** |
| جداول في الـschema | **71** |
| أسطر `schema/index.ts` | **2802** |
| ملفات اختبار | **80** — تكامل حقيقي على Postgres |

**تقييمي بصراحة: الباك إند أقوى طبقة في المشروع.** 80 ملف اختبار بيشتغلوا
على Postgres حقيقي في CI وطلعوا خضر. **مفيش فيه أي بند من تقرير الضرر.**
كل الضرر كان في الموبايل والعمليات.

---

# ٤) 🔴 النشر — فجوة تكافؤ خطيرة

## الجدول

| الهدف | api | web | **banco-website** (الحي) | banco-web (**مجمّد**) |
|---|---|---|---|---|
| **coolify** | ✅ | ✅ | ✅ | ✅ |
| **aws** | ✅ | ✅ | 🔴 **❌** | ✅ |
| **gcp** | ✅ | ❌ | ❌ | ❌ |

## الخطر بالنص

**`deploy.yml` — وهو مسار الإنتاج على AWS — بيشير لـ`banco-web` بس:**
```bash
grep -oE "Dockerfile\.[a-z-]+|banco-web|banco-website" .github/workflows/deploy.yml
# Dockerfile.api
# Dockerfile.web
# banco-web          ← المجمّد
```

و`banco-web` فيه ملف اسمه حرفيًا:
```
artifacts/banco-web/FROZEN.md
"# FROZEN — do not extend"
"→ website work goes to artifacts/banco-website"
```

### يعني: **مسار الإنتاج على AWS بينشر التطبيق المجمّد، مش الحي.**

**اللي بيحمينا دلوقتي بالصدفة:** `deploy.yml` بيولّع على أوسمة
(`tags: ["v*.*.*"]`) — **والريبو ده فيه صفر أوسمة**. فالمسار معطّل.

**لكن أول ما يتعمل وسم، هيشحن `banco-web` المجمّد.**

**gcp أسوأ:** عنده `Dockerfile.api` بس. مفيش أي واجهة.

**coolify هو الهدف الوحيد المكتمل** — وهو اللي المستندات بتوصّي بيه فعلًا.

---

# ٥) الدوكر — الجرد

```
deploy/coolify/  Dockerfile.api · web · banco-web · banco-website   ← الوحيد المكتمل
deploy/aws/      Dockerfile.api · web · banco-web
deploy/gcp/      Dockerfile.api
الجذر:           docker-compose.coolify.yml · prod.yml · test.yml
```

**خدمة `migrate` في compose بتاع coolify سليمة** — بتطبّق الميجريشن بالترتيب
مش `push --force`، وموثّقة بالأمر الكامل في رَنبوكين.

---

# ٦) الأسباب الحقيقية — بلا مجاملة

## س-١ · الفرع المختلط
الخرايط (429 سطر · صفر تعارض) محبوسة **بسبب ملفات استيراد مالهاش علاقة**
على نفس الفرع.

## س-٢ · «اتعمل» ≠ «نزل»
تلات بنود من جردي **اتنفّذوا بالفعل** — `REPLACE_` · حارس الاستيراد ·
`ensureSchemaPatches`. **كلهم محبوسين.** الوكلاء بيعتبروا الكوميت هو
النهاية. **النهاية هي `main`.**

## س-٣ · الشغل بلا حجز
خمس ازدواجات. البروتوكول موجود ومكتوب من 08-02 — **ومحدش بيلتزم بيه**.

## س-٤ · الكتابة أسهل من التنفيذ
39% من الكوميتات مستندات · 138 ملف · 2.9 ميجا.
**بند «تواريخ الحجز» اتوثّق في جردي 08-03 ولحد النهاردة محدش كتب سطر كود
فيه** — بينما اتكتب عنه في تلات مستندات.

## س-٥ · موانع حقيقية اتخلطت بموانع وهمية
**الحضور بصريًا** مانع حقيقي (Clerk). **لكن باقي البنود مالهاش مانع** —
مجرد إنها محتاجة حد يدمج. **خلط الاتنين بيخلي كل حاجة تبان محجوزة.**

---

# ٧) خمس خطوات — كلها ميكانيكية

| # | الخطوة | العائق التقني |
|---|---|---|
| 1 | دمج `are-you-here-84u6fs` | **صفر** |
| 2 | فصل الخرايط في فرع لوحدها ودمجها | **صفر تعارض** |
| 3 | دمج الباقي بترتيب التعارض المختبَر | 7 تعارضات محلولة ومكتوبة |
| 4 | إضافة `Dockerfile.banco-website` لـaws | ملف واحد |
| 5 | تصحيح `deploy.yml` من `banco-web` لـ`banco-website` | **قرارك — ده بيغيّر اللي بينشحن للإنتاج** |

---

**تقييمي النهائي بصراحة:** المشكلة في المشروع ده **مش في جودة الكود**. الباك
إند ممتاز، وشغل الوكلاء الأخير (الخرايط · الهيدرز · طبقة الرندر) شغل حقيقي
ومُثبت. **المشكلة إن الشغل بيتعمل وبيتوقف قبل `main` بخطوة واحدة** — والخطوة
دي ميكانيكية، مش تقنية.

— الوكيل المساعد
