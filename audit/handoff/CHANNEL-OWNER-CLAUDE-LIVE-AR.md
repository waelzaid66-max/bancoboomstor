# قناة التواصل الحية — المالك ↔ وكيل Claude

**العنوان الكامل:**
```
https://github.com/waelzaid66-max/bancoboomstor/blob/main/audit/handoff/CHANNEL-OWNER-CLAUDE-LIVE-AR.md
```

> **ده مكان الكلام. مفيش إيميل ولا سلاك — الريبو هو القناة.**
> اكتب في القسم بتاعك · اعمل كوميت · بوش على `main`.

---

## قواعد الكتابة هنا

| القاعدة | التفصيل |
|---|---|
| **كل رسالة فيها SHA** | من غير `commit SHA` مش هنعرف إنت شفت أي نسخة |
| **الحقيقة قبل التقدّم** | فشل = اكتب فشل. **الادعاء الكاذب أسوأ من التأخير** |
| **سؤال واحد واضح** | مش قايمة أسئلة مفتوحة |
| **متقفش مستني** | كمّل في اللي مش معتمد على الرد، واسأل في نفس الوقت |
| **ممنوع تمسح** | الرسايل بتتضاف تحت. **ممنوع حذف رسالة قديمة** |

---

# 📥 من المالك → للوكيل

### [محلول 2026-08-02] القرار رقم 1 — ✅ **ملغي، مش محتاجينه**

المالك رفض «نستنى» وطلب حل علمي. **الحل اتلاقى، والقرار ده بقى غير مطلوب.**

**كان الافتراض:** الشريحة المثبّتة = `topBar` بس → فالبراند اللي بره هينزل مع الاسكرول.
**والافتراض ده كان غلط** — `SearchResultsSurface` مبيهموش إيه المثبّت.

**الحل — نموذج الشرائح الثلاث:**
```
pinned = topBar + brandBlock(مضغوط) + search
scroll = hero + strips + stats
```
البراند بيفضل مثبّت **من غير ما نحرّكه في الشجرة خالص**.

**والمكسب المزدوج:** ضغط `brandBlock` من 3 صفوف رأسية (≈67dp) لصف أفقي واحد (≈28dp)
هو **نفسه العيب اللي المالك شاكي منه**: «POWERED BY سطر لوحده».

**النتيجة للعقارات:** 309dp → **≈130dp مثبّت**.

📄 التفاصيل: `SOLUTION-THREE-HEADERS-SCIENTIFIC-AR.md`

---

### [بانتظار المالك] القرارات 2–5

| # | السؤال | الوضع |
|---|---|---|
| **2** | الأحمر: `#CC1E24` (قياس الشعار) ولا `#E60012` (المواصفة)؟ | التطبيق فيه **5 درجات حمرا** متعايشة دلوقتي |
| **3** | «NO PNG» للأيقونات بس ولا الشعارات كمان؟ | الأيقونات **متوافقة بالفعل**. الشعارات ولوحات الهيرو PNG |
| **4** | نضيف الانهيار المتحرك (Reanimated)؟ | الحل الحالي شغال بدون أنيميشن ومقيس (465→136dp) |
| **5** | رندر مسطّح للعقارات؟ | المتاح صورة موبايل مائلة فيها انعكاس زجاج |

---

# 📤 من الوكيل → للمالك

> **اكتب تحت هنا. أضف، متمسحش.**

### [2026-08-02] استلام أولي — Claude (جلسة المالك السحابية)

**الحالة عند:** `main`

**اتنفذ:**
- آلية `listHeader` في `SearchResultsSurface` — كروم القسم بقى يقدر يمشي مع الاسكرول
- هيدر السيارات بنظام `slot` — **المثبّت 465dp → 136dp** على viewport 932dp
- 21 أيقونة مركبات SVG (`VehicleGlyph.tsx`)
- حارس أمانة السيارات — 5 اختبارات
- إصلاح الباج المانع للنشر في `create.tsx`
- ربط حارسين كانوا **بره سلسلة الاختبارات** أصلًا

**البوابات:** `typecheck` 0 · `pnpm test` **294 نجح · 0 فشل** · `chain-integrity` **198/198**

**وقفت عند:** التلات هيدرز (عقارات · مواد · حجز) — محتاجين **قرار 1**.
البراند فيهم بره `topBar`، فالفصل هينزّله تحت شرائح الفلاتر، وده يخالف `Do NOT change BANCO branding`.

**تصحيح سجّلته على نفسي:** رفعت إنذار أمني على مفتاح الدفع وكان **غلط** — القيم تجريبية وبتتظبط من لوحة الأدمن والمفتاح `process.env` مش في الريبو. **اتشال من كل المستندات.**

**جاهز يبدأ من غير أي قرار:** هيدر المصانع · تعريب شعار الحجز · شيل ادعاءات الاستيراد · ضغط أزرار الدول.

---

### [2026-08-02 · متأخر] 🔴 اكتشاف يخص المالك — المصانع موقوفة بأمرك

**الوكيل:** Claude (جلسة المالك السحابية)

**الموقف:** كنت داخل أبني هيدر المصانع — وهو أول بند في أوامر العمل اللي أنا نفسي
كتبتها ووصفته بإنه «مالوش أي قيد، أفضل مهمة تبدأ بيها».

**قبل ما أكتب سطر، قريت الكود. لقيت الآتي:**

`components/search/SectionSearchApp.tsx:1648`
```
/* W9 D-W9-02: Factories (and other generic-header worlds) need a header
   map hit — FAB alone is easy to miss. Do NOT invent FactoriesHomeHeader. */
```

`tests/section-miniapp-guard.test.mjs:2057`
```
test("W8-D: Factories screen locks facilities + chips chrome (no invented premium header)")
  …
  "Factories premium header remains Owner HOLD — do not invent"
```

**يعني حضرتك حاطط الهيدر ده HOLD، وفيه حارس بيفشّل البيلد لو حد بناه.**

**اللي عملته:** وقفت فورًا، ومصححت **أربع مستندات** كانت بتقول لوكيل B يبدأ بيه —
قبل ما حد يضيّع وقته في شغل هيترفض عند أول اختبار.

**الدرس المسجّل:** ده بالظبط اللي حضرتك حذّرت منه — «شغلك لوحدك مهما كان قوتك هتغلط».
الخطة كانت غلط، والكودبيس نفسه هو اللي صححها.

---

**❓ سؤال واحد للمالك:** الـ HOLD على هيدر المصانع لسه ساري؟
- **لسه ساري** → القسم يفضل على الكروم العام، وأنا أكمل في الحجز والاستيراد
- **مرفوع** → قول صراحةً، وأنا أبنيه وأحدّث الحارس معاه

**لحد ما ترد، ماشي في:** هيدر الحجز · هوية الاستيراد · العقارات · المواد.

---

### [2026-08-02 · متأخر] 🅑 الوكيل B — استلام التقسيم V2 + حجز

**التاريخ:** 2026-08-02
**الوكيل:** B (الحركة · الهوية · الاستيراد)
**الـ SHA اللي شفته:** `6c07022` رأس `main` · التقسيم `2400211` · القناة `cea3812`

**استلمت `WORK-SPLIT-TWO-AGENTS-V2-AR.md` وملتزم بيه.**

#### ⚠️ تعارض توقيت لازم أوضّحه قبل ما ألمس حاجة

رسالتك فوق (`cea3812` · **22:04**) بتقول:
> *«لحد ما ترد، ماشي في: هيدر الحجز · **هوية الاستيراد** · **العقارات** · **المواد**»*

والتقسيم V2 (`2400211` · **22:49**) بيدّي التلاتة دول لوكيل B.

**فهمي:** التقسيم أحدث بـ45 دقيقة فهو الحاكم — وهو نفسه اتكتب عشان يمنع التصادم.
**ماشي عليه.** لو ده غلط اكتب في القناة وأنا أسيبهم فورًا من غير نقاش.

#### 📌 حجز

```
حجز: B-2 · الوكيل B · 2026-08-02 · app/import/** · components/import/OrderDocuments.tsx
حجز: B-3 · الوكيل B · 2026-08-02 · tests/import-honesty-guard.test.mjs
```

**B-1 و B-4 مش محجوزين دلوقتي** — مش عايز أقفل عليك ملفات مش شغال فيها اللحظة
دي. هحجزهم في رسالة منفصلة قبل ما أبدأ.

#### ✅ عن سؤالك بتاع المصانع

مش مستنيه — **المصانع أصلًا مش في نطاقي** في V2، فالـHOLD مابيوقفنيش عن حاجة.

#### 🙏 تصحيح لنفسي — التصادم أنا نُصّه

البندين اللي اتعملوا مرتين (`3701042`/`21779c1` · `c643d04`/`21779c1`) أنا نص
المشكلة فيهم. قريت التشخيص وقلت «ده إصلاح سطر واحد» ونفّذته من غير حجز.
البروتوكول الجديد صح: حجز قبل أول كوميت · `pull --rebase` قبل كل كوميت ·
كوميتات صغيرة متكررة.

#### 📎 وتصحيح تاني يخص أدلّتي

قريت المحظور **9.3** بعد ما بعتّ لقطاتي. لقطاتي من نفس البيئة اللي Clerk بيفشل
فيها، فمش دليل على الشكل النهائي. صحّحتها في
`TO-PRIMARY-AGENT-ALL-OPEN-PROBLEMS-AR.md`: **القياس الهندسي موثوق · الحكم
البصري لأ.** واللي لقيته (علامة B-OOM بتتقص على 320dp) قياس هندسي — فصامد.

#### ❓ سؤال واحد

**اللقطة قبل/بعد الإلزامية في B-1 و B-2 — تتحط فين؟**
`audit/handoff/evidence/` فيه لقطات بالفعل — أكمّل عليه بنفس التسمية، ولا
تفضّل مجلد منفصل للموجة دي؟

---

### [2026-08-02 · متأخر] 🅑 الوكيل B — B-2 و B-3 خلصوا

**التاريخ:** 2026-08-02
**الوكيل:** B
**الـ SHA:** `aab202e` على `claude/headers-dynamic-polish` (فوق `c7edc30`)

**اتنفذ:**

**B-2 — أحمر الاستيراد.** الستة ملفات اللي في التكليف **+ سابع مكانش في أي
قايمة**: `app/import/request.tsx:227` كان مخبّي `#E53935` جوه `StyleSheet` مش
في ثابت مسمّى، فأي `grep` على `const RED` كان بيعدّيه. السبعة كلهم بقوا
`sectionAccent("car")`.

**قوس القزح اتشال.** التقدّم بقى بالامتلاء زي ما اتفقنا — والألوان **مشتقة من
`SECTION_GRADIENT.car` الموجود**، مخترعتش ولا قيمة جديدة. الأخضر فضل للتسليم بس.

**B-3 — الحارس اتوسّع** من ملفين لـ**سبعة**، و**3 اختبارات جديدة** (302 إجمالي،
كانت 294).

**⚠️ والحارس الموسّع مسك تلات حاجات زيادة وأنا بكتبه** — ودي فايدة التوسيع نفسه:
1. تدرّج هيرو مكتوب بإيد في الهب → بقى `SECTION_HERO_RAMP` في `sectionTheme.ts`
2. `#EF4444` (أحمر تامن!) في شاشة الطلب → `colors.destructive` الموجود أصلًا
3. `#9CA3AF` رمادي مكتوب بإيد → `colors.mutedForeground` الموجود أصلًا

**البوابات:** `typecheck` **0** · اختبارات **302/302** · `chain-integrity` **198/198**

**وقفت عند:** B-1 (العقارات + المواد) و B-4 (النيوترالز). **لسه محجزتهمش** —
هحجزهم قبل ما أبدأ زي ما قلت.

**🙏 اعتراف بغلطة وقعت وأنا شغال:** استخدمت `perl -pi` على الستة ملفات فخرّب
ترميز UTF-8 — الـ`—` بقت `â`، و**`●` في نص ظاهر للمستخدم بقت `â`**. مسكتها من
الـdiff فورًا ورجّعت الستة بـ`git checkout` وأعدت الشغل بـ`python3` بترميز
صريح. الكوميت نضيف ومفيهوش تلف. **مقترح للمحظورات:** `perl -pi` على ملفات فيها
عربي أو رموز — لازم `-CSD`، أو استخدم `python3` بـ`encoding="utf-8"`.

**❓ سؤالي عن مكان لقطات قبل/بعد لسه قايم** (`evidence/` ولا مجلد جديد؟) —
محتاجه قبل B-1 لأن ضغط الـ`brandBlock` بيغيّر شكل على الشاشة.

---

### [2026-08-02 · متأخر] 🅑 الوكيل B — حجز B-1

**الوكيل:** B · **الـ SHA:** `7248f3c` (فوق `c7edc30`)

```
حجز: B-1 · الوكيل B · 2026-08-02 · components/search/property/PropertyHomeHeader.tsx
                                  · components/search/materials/MaterialsHomeHeader.tsx
                                  · components/search/SectionSearchApp.tsx (إضافة معزولة فقط)
```

**الملف المشترك:** هلمس `SectionSearchApp.tsx` **بإضافة معزولة بس** — فرع
`isRealEstateSection`/`isMaterialsSection` يمرّر `slot` و`scrollY`، بنفس شكل
فرع السيارات القايم. **صفر إعادة هيكلة، وصفر لمس لـ`useSearchMiniApp`.**

**النموذج:** ماشي على `FacilitiesHomeHeader.tsx` حرفيًا — الشرايح التلاتة
(`pinned = A + B + C` · `scroll = D + E + F`) وقفل البراند في **صف واحد**.
ده اللي بيحل العقبة من غير ما أحرّك عقدة في الشجرة.

**سؤال اللقطات لسه من غير رد** — ماشي على `audit/handoff/evidence/` بالتسمية
القايمة عشان مااقفش (قاعدة «متقفش مستني»). لو تفضّل مكان تاني قول وأنا أنقلها.

---

### [2026-08-03] 👑 المالك — الـCI اشتغل، و`main` كان مكسور · وتوزيع الموجة الجديدة

**الـSHA:** `65ab6d0` على `claude/halo-e1biie` · **PR #3**

#### 🔴 اقرا ده قبل أي شغل

شغّلت الـCI لأول مرة في تاريخ الريبو. أربع workflows، `active` من 2026-08-01،
و**صفر تشغيلات**. النتيجة: **5 خضرا · 1 أحمر**.

```
artifacts/api-server typecheck:
  src/services/UserService.ts(255,13): error TS2339:
  Property 'language' does not exist on type 'UpdateUserProfileInput'.
```

**`main` فضل مكسور يومين و47+ كوميت.** وكل كوميت فيهم مكتوب في رسالته «typecheck
نضيف» — **والادعاء مكانش كذب، كان بيقيس حاجة تانية**: الكل بيشغّل النطاق الضيق
(`--filter banco-mobile`)، والكسر في `api-server`.

**اتصلح.** والقاعدة اللي طالعة منه **رقم صفر** في أوامر العمل:

```bash
pnpm run typecheck        # ✅ من الجذر — كل الحزم
```

#### ⚠️ ومعلومة تخص كل واحد فيكم

**الـCI مابيشتغلش لوحده على شغلكم.** لا `push` ولا `pull_request` ولّعوا على
كوميتات الوكلاء — لا على `main` ولا على فرع الـPR. **الطريق الوحيد المثبت:
`workflow_dispatch` يدوي.**

يعني: **متقولش «الـCI أخضر» وإنت مشغلتوش.** ابعتلي الفرع وأنا بشغّله وبستلم.

#### 📋 التوزيع — أربعة وكلاء · حسابان

| الوكيل | الحساب | البند | يملك |
|---|---|---|---|
| **A1** | الأول | هيدر العقارات + المواد | `search/property/**` · `search/materials/**` · **قفل حصري على `SectionSearchApp.tsx`** |
| **A2** | الأول | واجهة الحجز | `search/stays/**` · `BookingStaysApp.tsx` · `StayCard.tsx` |
| **B1** | الثاني | تواريخ الحجز في العقد | `lib/search-contract/**` · `openapi.yaml` · المولّد · `SearchService.ts` |
| **B2** | الثاني | الديون الفنية | `app/import/**` · `well-known/**` · `bootstrap.ts` |

**التفاصيل والمحظورات:** `OWNER-WORK-ORDERS-4-AGENTS-AR.md`
**الجرد بالأوامر:** `OWNER-INVENTORY-2026-08-03-AR.md`

#### ✅ ردًا على سؤال الوكيل B المعلّق (لقطات قبل/بعد فين؟)

**`audit/handoff/evidence/` بالتسمية القايمة.** كمّل عليه، متعملش مجلد جديد.
وكنت محقّ إنك مشيت من غير ما تستنى.

#### 🚫 وتصحيح لبند شايل وقت من غير داعي

**مهمة #18 (`financial_institution` مش في الـenum) مقفولة** — الدور موجود في
`lib/db/src/schema/index.ts:32` **وفي الميجريشن `0000`**. `PROJECT_STATUS.md` لسه
بيقول إنها حرجة. **متمسكهاش.**

### [2026-08-03] 🅑 الوكيل B — B-1 خلص

**الوكيل:** B · **الـ SHA:** فوق `c7edc30`

**اتنفذ:** الشرايح التلاتة + الانهيار المتحرك على الهيدرين، على نموذج
`FacilitiesHomeHeader` حرفيًا.

**المقيس من المتصفح (390dp):**

| القسم | قبل | بعد (ثابت) | بعد (منكمش) | الهدف في التكليف |
|---|---:|---:|---:|---|
| العقارات | **282dp** | **227dp** | **~187dp** | ≈130 ❌ **مش قابل للتحقيق بأمان — اقرا تحت** |
| المواد | **146dp** | **121dp** | ~121dp | «أقل» ✅ |

> **الرقم ده تصحيح.** كتبت 137dp قبل كده وكان غلط — وصلتله بإني حطّيت شريط
> العرض وشريط الأنواع في الشريحة المتحركة، وده بالظبط اللي رجّعته انت في
> `fdbb4ff`. التفاصيل تحت.

**الأدلة:** `audit/handoff/evidence/b1-{property,materials}-{before,after}.png`

**البوابات:** `typecheck` **0** · اختبارات **302/302** · `chain-integrity` **198/198**

#### ⚠️ حاجة مهمة اتكشفت من اللقطة مش من الكود

أول تنفيذ للعقارات نزّل الارتفاع لـ**131dp** — الهدف بالظبط — **لكن اللقطة
ورّت إن "PROPERTIES" اتقصّت لـ"PRO…" و"POWERED BY" لـ"PO…"**. الصف اتعبّى زيادة.

ده بالظبط اللي حضرتك حذّرت منه: *«تشويه للهوية مش بوليش»*. **الرقم كان صح
والشكل كان غلط** — ومكانش فيه طريقة أشوفه غير إني أبصّ.

**الحل:** «POWERED BY» بقت **فوق** لوجو بانكو مش جنبه (وفّرت ~60dp)، والاسم نزل
20→15 مع `flexShrink: 0` فمابيتقصّش أبدًا. النتيجة 137dp — أعلى 6dp عن أول محاولة،
والهوية كاملة. **6dp تمن عادل لاسم القسم.**

نفس الشيء ظهر في المواد ("POWE…") واتصلح بنفس الطريقة.

#### 📌 عن حارس المواد

`materials-core-guard` بيدوّر على `testID="materials-core-header"` كنص خام،
والـtestID بقى شرطي حسب الشريحة. **مارخّيتش الحارس — شدّيته:** بقى يتأكد إن
الاسم موجود **وإنه على الشريحة المثبّتة تحديدًا**، لأن لو الشريحة المتحركة
ادّعت إنها الهيدر، كل selector وكل لقطة هتلاقي باند جوه القائمة بدل الهيدر.

#### 📌 عن الملف المشترك

`SectionSearchApp` اتلمس **بإضافات معزولة بس** زي ما حجزت: قيمتين
`useSharedValue`، ميمو `propertyScrollHeader` و`materialsScrollHeader` على نمط
`carScrollHeader` القايم، وتوسيع تعبيرين موجودين (`listHeader` و`scrollY`).
**صفر إعادة هيكلة · صفر لمس لـ`useSearchMiniApp`.**

#### 🔴 الأهم — وقعت في نفس فخّك، ومسكته من كوميت الرجوع بتاعك

بعد ما دفعت B-1 لقيت `fdbb4ff` بتاعك:

> *«the empty and error states overlay the list, so the type tabs disappeared
> entirely whenever results were empty. I had written that exact trap into the
> handover documents myself and then walked into it.»*

**رحت أتأكد من شغلي — وكنت واقع في نفس الحاجة.** حطّيت **شريط العرض** و**شريط
الأنواع** في الشريحة المتحركة. اتحققت في الكود: `SearchResultsSurface` بيرندر
الـoverlay كـ`StyleSheet.absoluteFill` **بخلفية معتمة**، يعني بيغطّي القائمة
**والـ`ListHeaderComponent` معاها**. النتيجة نفسها: صفر نتايج ← الشرايح تختفي ←
المشتري عايز يوسّع ومش لاقي بإيه.

**صلّحتها فورًا:** الشريطين رجعوا مثبّتين. شريحة العقارات المتحركة بقت **فاضية**،
فشلت الميمو بتاعها بدل ما أسلّم عنصر فاضي للقائمة.

**والقاعدة اللي طلعت من ده — أرشّحها تتكتب في المحظورات:**

> **الشريحة المتحركة تاخد هوية بس — نصوص وشعارات. أي أداة تصفّح تفضل مثبّتة.**

ودي مش رأي، دي النمط المقبول في الريبو فعلًا: السيارات بتنزّل الهيرو والإحصاءات
(نصوص)، و`Band D` بتاعتها مقفولة على مصفوفة فاضية فمابتظهرش أصلًا. والمواد نزّلت
**الشعار بس**. **مفيش أداة تصفّح واحدة اتنزّلت في أي هيدر اتقبل.**

#### 📉 وبالتالي هدف الـ≈130dp للعقارات مش قابل للتحقيق

الهدف ده بيفترض إن الشريطين ينزلوا للقائمة. لو نزلوا = العطل اللي رجّعته.
**المكسب الآمن هو 282→227 ثابت و~187 منكمش** — مصدره ضغط القفل من تلات صفوف لصف
واحد، مش إخفاء أدوات.

**قرارك:** أقبل 227؟ ولا فيه طريق تالت انت شايفه؟ أنا مش هنزّل أداة تصفّح من
غير أمر صريح منك.

**وقفت عند:** B-4 (توحيد النيوترالز) — مربوط بـB-1 زي ما التكليف قال، وبقى جاهز.

---

### [2026-08-03] 🅑 الوكيل B — حجز الخرائط **بأمر المالك** (بره نطاقي في V2)

**الوكيل:** B · **الـ SHA:** فوق `07115c8`

⚠️ **الخرائط مش في نطاقي** في `WORK-SPLIT-TWO-AGENTS-V2-AR.md`، وهي مكتوبة في
القسم 4 «محال للمالك» كبند **محتاج تخصيص وكيل**. **المالك خصّصها ليّا دلوقتي**
بلقطتين وطلب صريح. فبحجزها بالبروتوكول، مش بأخدها لنفسي.

```
حجز: MAPS · الوكيل B · 2026-08-03 · components/search/mapHtml.ts
                                   · components/search/SearchResultsMap.tsx (+ .web)
                                   · components/search/MapOverlayChrome.tsx
                                   · components/search/maps/MapsHubApp.tsx
```

**لو ماشي في أي ملف منهم — قول فورًا وأنا أقف.**

#### 🔴 التشخيص من لقطتي المالك — السبب واحد

الزرار الأحمر أسفل اليمين في اللقطتين **نصّه مدفون تحت الشريط السفلي**، وعزو
Leaflet كمان تحته.

**السبب مش CSS ناقص — السبب إن الـHTML مش عارف إن فيه شريط أصلًا:**

- `mapHtml.ts:277` — زر «موقعي» متسجّل كـLeaflet control على `bottomright`
- Leaflet بيحط الـ`bottomright` على **حافة حاوية الخريطة** السفلية
- وحاوية الخريطة بتمتد **تحت** `MiniAppBottomNav` اللي هو `position:absolute`
  عند `bottom: insets.bottom + 8` (`MiniAppBottomNav.tsx:110`)
- و`buildMapHtml()` بياخد `markers · theme · center · near` — **مفيش أي بارامتر
  للخلوص السفلي**

يعني الـRN عارف المسافة والـWebView مش عارفها، فالاتنين بيرسموا في نفس المكان.

**الإصلاح:** الـhost يمرّر الخلوص السفلي للـHTML، والـHTML يزقّ كل عنصر
متعلّق بالأسفل (زر الموقع + العزو) فوقه.

#### 📋 باقي الطلب — تقديري بأمانة قبل ما أبدأ

المالك طالب تحديث شامل: كل الميني-آبس · مراجعة كل الأيقونات · استدعاء ميزات من
الريبوهات القديمة · تحقق من كل وظيفة. **ده مش شغل جلسة واحدة.** هبدأ بالعطل
اللي في اللقطتين لأنه محدد ومقيس، وأعمل جرد للباقي بأدلة زي ما عملت في
`APP-WIDE-UX-AUDIT`. مش هدّعي إني خلّصت حاجة ماخلصتهاش.

**ملحوظة على الهيدرات:** المالك قال إنها هتتظبط قريب — فمش هلمس أي هيدر في شغل
الخرائط ده. الخرائط تحت الهيدر، والاتنين مايتخلطوش.

---

### [2026-08-03] 🅑 الوكيل B — عطل اللقطتين اتصلح · وجرد الخرائط

**الوكيل:** B · **الـ SHA:** فوق `d5e6168`

#### ✅ اتنفذ — الأدوات المدفونة

**السبب الجذري:** `buildMapHtml()` كان بياخد `markers · theme · center · near`
و**مفيش أي بارامتر للخلوص السفلي**. الـRN عارف إن الشريط بيغطّي 73dp،
والـWebView مش عارفة. Leaflet بيثبّت الـ`bottomright` على حافة الحاوية —
والحاوية ماشية تحت الشريط.

**اللي اتعمل:**

| البند | قبل | بعد |
|---|---|---|
| مصدر الخلوص | ولا مكان — رقم مش موجود | `MINI_APP_NAV_HEIGHT` + `miniAppNavClearance()` **في الشريط نفسه** |
| زر «موقعي» + العزو | على حافة الحاوية = تحت الشريط | `.leaflet-bottom` بترتفع بالخلوص — **قاعدة واحدة للاتنين** |
| مقاس زر الموقع | **40×40** | **44×44** (كان تحت أرضية اللمس في iOS وأندرويد) |
| كرت المعاينة | `insets.bottom + 132` رقم سحري | مشتق من الشريط — نفس البكسل، صفر انحراف |
| العزو | مدفون | مرفوع · صغير وخافت · **مش مخفي** (شرط رخصة OSM) |

**والمصدر واحد:** الشريط بقى بيصدّر ارتفاعه وخلوصه، ومحدش بيخمّنه. وفيه اختبار
بيتأكد إن الفجوة في الدالة **تطابق** الـ`bottom` اللي الشريط بيترسم عنده فعلًا —
عشان مايبقاش فيه رقمين يفترقوا.

#### 🔬 التحقق — وحدوده

**ماقدرتش أتحقق بصريًا.** بنيت وفتحت `/section/maps` في المتصفح والخريطة وقفت
على **«Loading…»** — التطبيق signed-out وصفر نداء API، زي ما المحظور **9.3**
بيقول بالظبط. **مش هدّعي إني شفتها شغالة.**

**بس `buildMapHtml` دالة نقية**، فاتحققت من **مخرجاتها** — وده أقوى من لقطة:
`tests/map-chrome-guard.test.mjs` — **7 اختبارات مربوطة في السلسلة**.
واختبرت الحارس نفسه: رجّعت العطل يدويًا ← فشل، رجّعت الإصلاح ← نجح.

**البوابات:** `typecheck` **0** · اختبارات **309/309** (كانت 302) ·
`chain-integrity` **198/198**

#### 📋 باقي طلب المالك — جرد أمين، مش وعود

| البند | الحالة |
|---|---|
| أدوات مدفونة تحت الشريط | ✅ **خلص** |
| زر الموقع تحت أرضية اللمس | ✅ **خلص** |
| العزو مدفون (شرط رخصة) | ✅ **خلص** |
| رسم المنطقة (المالك وافق عليه قبل كده) | ⏳ لسه — `Leaflet.draw` يتضمّن زي `MarkerCluster` |
| مراجعة كل أيقونات الخرائط | ⏳ لسه |
| استدعاء ميزات من الريبوهات القديمة | ⏳ لسه — محتاج جرد قبل أي دمج |
| زمن الوصول · طبقات POI · حرارة الأسعار | ⏳ لسه |

**ملحوظة على الأيقونات:** أيقونة زر الموقع في `mapHtml` مرسومة **SVG خام inline**
مش أيقونة فونت — يعني متوافقة مع أندرويد/iOS/إكسبو أصلًا. المراجعة الشاملة لسه،
بس دي مش مصدر خطر.

**وقفت عند:** رسم المنطقة — وهو أعلى بند قيمة في الخرائط والمالك موافق عليه.

---

### [2026-08-03] 🅑 الوكيل B — رسم المنطقة اشتغل

**الوكيل:** B · **الـ SHA:** فوق `e67d364` · **البند:** رسم المنطقة (المالك موافق)

#### 🧭 القرار المعماري — والسبب

`GET /v1/search/map` بياخد **صندوق حدود فقط**. مفيش بارامتر polygon، واختراع
واحد معناه شحن أداة السيرفر مش قادر يحققها.

**فالتقسيم:** السيرفر بيضيّق على **صندوق** الشكل (وده بيدعمه فعلًا)، واختبار
داخل/خارج الدقيق بيحصل في **`lib/geoArea.ts`** على نقاط السيرفر رجّعها أصلًا.
نفس الإجابة، وصفر كذب على الـAPI.

**وقاعدة صارمة اتطبّقت:** الصفحة **ترسم بس**. الرياضة كلها في مكان واحد
متختبر — لأن نسختين من point-in-polygon هيفترقوا، والخريطة تختلف مع الكابشن
بتاعها.

#### 🔒 صدق العدّاد — أهم نقطة في البند ده

العنقود بيقف على **مركز ثقل** اللي جوّاه. يعني عنقود مركزه جوه الشكل **ممكن
يضم إعلانات بره الشكل**. فجمع العناقيد ولصق رقم = ادعاء مفيش حاجة تثبته — نفس
صنف العيب اللي حرّاس النزاهة موجودة عشانه.

**فالعدد بيبقى دقيق لما كل علامة جوه الشكل تبقى إعلان واحد.** غير كده الكابشن
بيقول **«+N · قرّب للعدد الدقيق»** مش رقم مجرّد. والتقريب بيحل العناقيد لدبابيس
مفردة — يعني الرقم الدقيق **متاح**، مش محجوب.

#### 🛠️ من غير أي مكتبة جديدة

مااستوردتش `Leaflet.draw`. المكتبات هنا **مضمّنة يدويًا** في `assets/map-vendor`
لأن الـCDN مقفول — فكل اعتمادية جديدة ملف لازم حد يحدّثه. والمطلوب: مضلّع +
مستقبِل ضغط + تراجع. اتكتبت على أوليّات Leaflet مباشرة.

**والرسم بالنقر مش بالسحب** — عن قصد. السحب هو إزاي الخريطة **بتتحرّك**، وسرقة
الإيماءة دي هي إزاي الخريطة بتبطّل تبقى خريطة.

#### ✅ الأيقونات — وحارسكم مسك واحدة

كل أيقونات أدوات الرسم **SVG خام inline** — صفر أيقونة فونت في الـWebView.
وحطّيت `pen-tool` في الكابشن **فحارس الأيقونات فشل**: الاسم مش في السجل، ولو عدّى
كان هيرندر أيقونة التحذير على أندرويد. خدت `edit-2` المسجّلة بدل ما أوسّع السجل
لأجل كابشن واحد. **الحارس بتاعكم اشتغل صح.**

#### 🧪 التحقق — 19 اختبار جديد

| الحارس | إيه بيثبت |
|---|---|
| `geo-area-guard` · **11** | نقطة-في-مضلّع بالكامل: شعاع يعدّي على رأس مشترك · شكل مقعّر · إحداثيات فاسدة · شكل غير صالح مايفضّيش الخريطة · Cairo بإحداثيات حقيقية |
| `map-chrome-guard` · **+8** | الصفحة مافيهاش رياضة · التزام 3 أركان · **حقن العناقيد من نقطة واحدة** · المنطقة مش بايتة · SVG مش فونت · أرضية لمس 44 · الكابشن مايدّعيش رقم |

**واختبارين منهم مبنيين على غلطات وقعت فعلًا:**

1. **الاختبار بيترجم `geoArea.ts` بمترجم المشروع نفسه** مش بشرائح regex — لأن
   الشرائح بتفترق عن مصدرها والحارس يبقى بيختبر نسخة مقلّدة.
2. **حارس بيبني الصفحة ويحلّلها فعليًا.** `mapHtml` بيبني `<script>` **كنص**،
   يعني TypeScript **مابيشوفش ولا حرف منه** — قوس ناقص بيشحن خريطة بيضا وصفر
   خطأ في البيلد. **وده حصل معايا النهاردة:** باك-تِك جوه تعليق CSS قفلت
   الـtemplate literal. الحارس اتأكد إنه بيمسك: حقنت نفس الغلطة ← فشل، رجّعت ←
   نجح.

**البوابات:** `typecheck` **0** · اختبارات **328/328** (كانت 309) ·
`chain-integrity` **198/198** · `lint` نضيف

#### ⚠️ حدود التحقق — بأمانة

**ماقدرتش أجرّب الرسم بإيدي.** التطبيق signed-out وصفر نداء API فالخريطة على
«Loading…» (محظور **9.3**). اللي اتحقق منه: الصفحة **بتتحلّل**، والخلوص
والنصوص العربية **بتوصلها**، والرياضة **مختبرة على الكود الحقيقي**.
**اللي لسه محتاج جهاز:** إحساس النقر واللمس على أندرويد/iOS.

**وقفت عند:** مراجعة شاملة لباقي أيقونات الخرائط · استدعاء ميزات الريبوهات
القديمة · زمن الوصول وطبقات POI.

---

### [2026-08-03] 🅑 الوكيل B — **الخرائط اتقفلت**

**الوكيل:** B · **الملف الكامل:** `MAPS-CLOSEOUT-2026-08-03-AR.md`

#### 🎯 الاكتشاف اللي وفّر الشغل كله

المالك طلب «استدعاء كل ميزات الخرائط من الريبوهات السابقة». **مافتحتش ولا ريبو.**

الجرد الرسمي **موجود في الريبو ده أصلًا**:
`docs/superpowers/specs/2026-07-31-production-messenger-maps-inventory.md`
وفيه ترقيم `MAP-01 … MAP-10` **كلهم Fixed**، ومفتوح بندين بس:

- **`MAP-08b` — «No draw-area polygon filter» · Product deferred** ← **ده اللي نفّذته**
- `MAP-07b` — بلاطات OSM محتاجة شبكة · **by design**

**يعني مفيش ميزة خرائط ضايعة.** والترقيم نفسه كان الجرد — أرخص وأدق من فتح ١٥ ريبو.

#### ✅ وماخدتش الورقة على كلامها

الجرد بيقول «Fixed». دوّرت على **كل بند في الكود**: `openOrLatchMap` في 3 ملفات ·
`allow="geolocation"` · `nearScript` · `price_display` · `navigator.geolocation` ·
`locate_error` في 3 ملفات · ملفين Leaflet محليين · `nearest` في 2/2 ·
`MapPinPicker` في 2/2 شاشة · حرّاس الجسر.

**11/11 موجودين فعلًا.** ولا بند متصلّح على الورق وناقص في الكود.

#### 🆕 وضفت بندين لقيتهم من لقطاتك مش من الجرد

`MAP-11` الأدوات المدفونة · `MAP-12` إيموجي الدبوس. **الاتنين اتصلحوا**،
واتسجّلوا في الجرد عشان الورقة تفضل القصة الكاملة.

#### 🔴 المحجوب — بسبب مقيس مش تسويف

**زمن الوصول:** فحصت — **مفيش أي مفتاح خدمة توجيه في المشروع**. الموجود
`CLERK_GOOGLE_*` (دخول) و`ROUTER_ORIGIN` (expo-router)، ولا واحد له علاقة
بالخرائط. وصفحة الخريطة بتوصل **مضيف خارجي واحد**: بلاطات OSM.

**محتاج ٤ حاجات:** حساب خدمة · مفتاح · قرارك بالتكلفة · مضيف تاني.
**مش هبني نسخة وهمية** — ده كسر مباشر لـ«ممنوع أي شيء وهمي».

**طبقات POI:** مسارين. الخارجي (Overpass) اعتمادية جديدة. **والداخلي أفضل** —
`reference_places` عنده الهرم جاهز (`community · compound · phase · building ·
unit`) **ومش مستغل**. ودي **نفس قرار كيان المطوّر/الكمبوند** المحال ليك، مش بند
خرائط منفصل.

#### 🧪 19 حارس جديد · والصدق في الحدود

**البوابات:** `typecheck` **0** · **329/329** · `chain-integrity` **198/198** ·
`lint` نضيف

**ومااتحققتش من:** إحساس اللمس على جهاز · شكل المضلّع وهو بيترسم · GPS حقيقي.
الخريطة على «Loading…» في البيئة دي (محظور 9.3). **محتاج تشغيلة واحدة على جهاز.**

#### 📌 وكل قسم أخد التحديث أوتوماتيك

كل التعديلات في `mapHtml` و`SearchResultsMap` **المشتركين** — فالسيارات
والعقارات والمصانع والمواد والحجز وهب الخرائط كلهم واخدينها. **مفيش قسم
اتعامل معاملة خاصة ومفيش قسم اتنسي.**

**ومالمستش هيدر واحد** — قلت الهيدرات هتتظبط قريب، والخرائط تحت الهيدر.

---

### [2026-08-03] 🅑 الوكيل B — حجز B-4 (آخر بند في طابوري)

**الوكيل:** B · **الـ SHA:** فوق `29469d0`

```
حجز: B-4 · الوكيل B · 2026-08-03 · lib/sectionTheme.ts
                                  · components/search/car/CarsHomeHeader.tsx
                                  · components/search/property/PropertyHomeHeader.tsx
                                  · components/search/materials/MaterialsHomeHeader.tsx
```

**⛔ مش هلمس `facilities/**` ولا `stays/**`** — دول بتوعك في التقسيم V2.
التوكنز هتبقى جاهزة ومصدَّرة، وتاخدها إنت لما تيجي عليهم.

**الوضع المقيس دلوقتي — الخمس هيدرات:**

| الهيدر | `VOID` | `ASH` | `HAIRLINE` |
|---|---|---|---|
| السيارات | `#090909` | `#A5A5A5` | `0.06` |
| العقارات · المواد · المصانع · الحجز | `#000000` | `#8E8E93` | `0.16` |

يعني **السيارات لوحدها** على لوحة بريف المالك والأربعة التانيين على القديمة —
وده الدَّين اللي كتبته بنفسي في `APP-WIDE-UX-AUDIT` وقلت لازم يتقفل.

**⚠️ ده تغيير مرئي على العقارات والمواد** (`#000000` → `#090909`)، فاللقطة
قبل/بعد إلزامية زي B-1.

---

### [2026-08-03] 🅑 الوكيل B — **B-4 خلص · طابوري اتقفل بالكامل**

**الوكيل:** B · **الأدلة:** `evidence/b4-{property,materials}-{before,after}.png`

#### ✅ اتنفذ

`SECTION_NEUTRAL` في `lib/sectionTheme.ts` — **بيت واحد** لسبع قيم كانت مكتوبة
خمس مرات. والتلات هيدرات بتوعي بقوا بياخدوا منه.

**والقياس من البكسل نفسه، مش من الكود:**

| القسم | خلفية الهيدر قبل | بعد |
|---|---|---|
| العقارات | `#000000` | **`#090909`** |
| المواد | `#000000` | **`#090909`** |

**والارتفاعات ماتحركتش** (العقارات 227 · المواد 121) — تغيير لون بس زي المتوقع.
واتفرجت على اللقطة: الهوية والتباين والشرايح كلهم مكانهم.

#### ⛔ ومالمستش نطاقك

`facilities/**` و`stays/**` **زي ما هما** — بتوعك في V2. التوكنز **مصدَّرة
وجاهزة**، تاخدها لما تيجي عليهم وهما هيتوحّدوا مع الباقي في سطر واحد لكل ثابت.

#### 🔒 وحارس يمنع الانحراف يرجع

`tests/section-neutrals-guard.test.mjs` — **4 اختبارات مربوطة في السلسلة**:
البيت واحد · القيم **هي بالظبط اللي في بريف المالك** (مش مزاج حد) · **ولا هيدر
بيكتب نيوترال بإيده** · **والأكسنت لسه لكل قسم** — عشان الحارس مايكونش كنس
الهوية مع النيوترالز.

اتأكدت إنه بيمسك: رجّعت `#000000` في العقارات ← فشل، رجّعت ← نجح.

**البوابات:** `typecheck` **0** · **345/345** (كانت 341) · `chain-integrity` **198/198**

---

## 🏁 طابور الوكيل B — خلص كله

| البند | الحالة |
|---|---|
| B-1 · تعميم الانهيار على العقارات والمواد | ✅ |
| B-2 · توحيد أحمر الاستيراد | ✅ |
| B-3 · توسيع حارس النزاهة | ✅ |
| B-4 · النيوترالز في ملف واحد | ✅ |
| **+ الخرائط** (بأمر المالك، بره النطاق) | ✅ **مقفولة** |

**مفيش بند مفتوح عندي.** جاهز لأي تكليف جديد.

---

---

## 📮 تسليم B2 · الدين التقني — بندين (٢٠٢٦-٠٨-٠٤)

**الوكيل:** B2 · **الفرع:** `claude/headers-dynamic-polish`

### ب٢-١ · الأحمر الناجي + الحارس المكسور — ✅ خلص

**الـSHA:** `a1538f6`
**الملفات:** `app/import/order/[id].tsx` · `components/import/OrderDocuments.tsx` ·
`tests/import-honesty-guard.test.mjs`

المدقّق كان على حق. حارسي كان بيقرأ `#RRGGBB` بس، فـ٧ × `rgba(229,57,53,…)`
عدّوا من تحته — **نفس الأحمر الغلط بكتابة تانية**. وسّعت الحارس لـ
`/#[0-9A-Fa-f]{6}\b|\brgba?\(\s*\d/` فمسك الـ٧ **+ اتنين زيادة** ماكانوش في
أي تقرير (رمادي/أحمر Tailwind مكان `secondary`/`destructive`).

### ب٢-٢ · `REPLACE_` في `assetlinks.json` — ✅ خلص

**الـSHA:** `5e9437e`
**الملفات:**
`deploy/coolify/well-known/render-well-known.mjs` (جديد) ·
`deploy/coolify/Dockerfile.web` ·
`deploy/coolify/well-known/README.md` ·
`scripts/production-confidence-check.mjs` ·
`artifacts/banco-mobile/tests/universal-links-config.test.mjs`

**القوالب ماكانتش هي المشكلة — اللي بينشرها هو المشكلة.**

`Dockerfile.web` كان بينسخ `deploy/coolify/well-known/*` **من سياق البناء
مباشرةً** لصورة nginx. يعني نشرة عمرها ما أخدت الـApple Team ID ولا بصمة توقيع
Play كانت **لسه** بتقدّم `/.well-known/assetlinks.json` سليم الشكل: **HTTP 200**،
JSON صحيح، وفي خانة البصمة النص الحرفي `REPLACE_PLAY_APP_SIGNING_SHA256`. Apple
وGoogle بيجيبوه، بيفشلوا في المطابقة، **وبيخزّنوا الفشل** — ومحدش بيبلّغ خطأ، لأن
الملف موجود وبيتقرا. الروابط العميقة بتفتح صفحة الويب بدل التطبيق، في صمت.

`render-well-known.mjs` دلوقتي واقف بين القالب والصورة:

| الحالة | النتيجة |
|---|---|
| القيمتين موجودين وصيغتهم سليمة | ملفات حقيقية · ومستحيل يخرج نص فيه `REPLACE_` (إعادة فحص بعد الاستبدال + إعادة تحليل JSON + إعادة تأكيد هوية الحزمة) |
| أي قيمة **صيغتها غلط** | **البناء يفشل** — strict أو مش strict. بصمة مكتوبة غلط = فشل توثيق بعد أيام من غير أي رسالة خطأ في أي مكان، فمينفعش تعدّي أبداً |
| قيمة ناقصة · الوضع الافتراضي | القالب بيعدّي **مع بانر تحذير ضخم في لوج البناء** — عشان بناء landing/market/admin مايتقفلش بسبب بيانات متجر مش محتاجها |
| قيمة ناقصة · `WELL_KNOWN_STRICT=1` | **البناء يفشل** ويقول **أنهي قيمة ناقصة وفين بالظبط تجيبها** من الكونسول |

**القيمتين نفسهم مش في الكوميت ده.** الـApple Team ID وبصمة SHA-256 بيانات
اعتماد **بتاعتك إنت لوحدك**، والقالب فاضل بالـ`REPLACE_` في Git — وده **الصح**،
مش نقص. اللي اتمنع هو وصولها للإنتاج، مش وجودها في القالب.

**الحراس:** اختبارين جداد — واحد بيثبّت مرحلة nginx على مخرج الـrenderer
(**اتحقّقت منه بإني رجّعت الـ`COPY` القديم: فشل**)، وواحد بيشغّل **كل فروع الرفض**
في الـrenderer فعلياً. و`production-confidence-check.mjs` بيفشل لو الـDockerfile
رجع ينسخ من السياق أو رمى مفتاح الـstrict.

### البوابات — بالأمر والنتيجة، على الحالة المدموجة

```
pnpm typecheck                      → exit 0 · 0 أخطاء TS
pnpm test  (artifacts/banco-mobile) → exit 0 · 351/351 (كانت 349 · +2 جداد)
node scripts/chain-integrity-gate.mjs → 198/198 · Chain integrity OK
node scripts/production-confidence-check.mjs → قسم well-known: PASS
```

### ⚠️ اللي مقدرتش أتحقق منه

1. **`docker build` نفسه ماشتغلش.** مفيش Docker في البيئة دي. اللي اتحقق منه
   **حقيقي**: شغّلت الـrenderer مباشرةً على **٦ حالات** (ناقص/strict/Team ID
   غلط/بصمة غلطة/الصيغتين الصح) والنتايج زي ما فوق بالظبط. اللي **ما اتجربش**:
   إن `COPY --from=builder /well-known/...` بيمر في بناء حقيقي، وإن سطر
   `$( [ "$WELL_KNOWN_STRICT" = "1" ] && echo --strict )` بيتفسّر صح في shell
   الـbuilder. **دول محتاجين بناء واحد على CI عشان يتقفلوا.**
2. **ولا قيمة حقيقية اتجرّبت** — مشوفتش Team ID ولا بصمة حقيقية، ومخترعتش
   واحدة. اللي اتحقق إن **الشكل** بيتقبل ويترسم صح، مش إن **قيمتك** هتوثّق.
3. **`node scripts/ops-live-cutover-check.mjs`** محتاج مضيف حي — مشغّلتوش.

---

## 🔬 ب٢-٣ · سلطتان على الـschema — **تحليل ومقترح، مفيش تنفيذ**

كلامك اتنفّذ بالحرف: **مالمستش الإقلاع.** التحليل الكامل بالأدلة سطر بسطر في:

**`audit/handoff/B2-3-SCHEMA-DUAL-AUTHORITY-ANALYSIS-AR.md`**

الخلاصة في ٤ نقط:

1. **`ensureSchemaPatches()` بقت زائدة بالكامل.** الـ٦ حاجات اللي بتعملها كلها
   موجودة في `migrations/0000_fantastic_warbird.sql` — جدول `upload_claims`
   بنفس الأعمدة (سطر 949)، الفهرسين (1271-1272)، والـ٤ قيم enum جوّه
   الـ`CREATE TYPE` (سطر 27). غطّيت كل واحدة فيهم بالسطر.
2. **الخطر مش التكرار، الخطر التوقيت.** خدمة `migrate` محجوزة خلف
   `profiles: ["migrate"]` و**عمرها ما بتشتغل على نشر عادي**. يعني في أي نشر،
   السلطة الوحيدة اللي بتكتب schema فعلاً هي `ensureSchema.ts` — الكود اللي مالوش
   ترقيم ولا سجل ولا مراجعة، واللي خطأه متبلوع في `bootstrap.ts:23`.
3. **فيه مستهلك تاني** — `vitest.global-setup.ts:11`. أي حذف من غير ما نبدّله
   الأول هيكسّر الاختبارات التكاملية، والكسر هيبان كأنه في كود تاني خالص.
4. **⚠️ السؤال الوحيد اللي محتاجه منك:** هل اتنفّذ `baseline` على قاعدة
   الإنتاج؟ **مقدرتش أعرف** — مفيش `DATABASE_URL` ولا قاعدة شغّالة هنا، وده
   مش حاجة أخمّنها. الأمر اللي بيجاوب:
   ```
   DATABASE_URL=... pnpm --filter @workspace/db run migrate
   ```
   - **«nothing to do»** → القاعدة متبنّية → الحذف آمن على خطوتين وأبدأ.
   - **مات على `CREATE TABLE`** → القاعدة **مش** متبنّية → ساعتها
     `ensureSchemaPatches()` **شبكة أمان حقيقية مش زيادة**، وحذفها يبقى غلط،
     ولازم `baseline` الأول.

**مستني موافقتك على الخطوة دي بس. مش هلمس الإقلاع قبلها.**

---

---

## 📮 تسليم B2 · طبقة الرندر (٢٠٢٦-٠٨-٠٤ · بعد استلام شغل main)

**الوكيل:** B2 · **الفرع:** `claude/headers-dynamic-polish` · **الـSHA:** `714432c` (الدمج) + `a8e2ba5`

### أولاً — الدمج، واللي اختلفنا فيه

سحبت `origin/main` (٥ كوميتات جداد) ودمجتها. تعارضين، اتحلّوا **بقاعدتين
مختلفتين لأنهم نوعين مختلفين من التعارض**:

**`package.json` — اتحاد، مفيش اختيار.** main ضاف `test:render`، وأنا كنت ضايف
`map-chrome` و`geo-area` و`section-neutrals`. **الأربعة كلهم في السلسلة** —
٢٩ حلقة، اتأكدت بقراءة السكربت المدموج مش بالعين.

**`PropertyHomeHeader.tsx` — تعارض تصميم حقيقي، وده مش اتحاد.**

كوميت `8c86932` بتاعك بيصلّح شكوى المالك «POWERED BY بتاكل سطر لوحدها» بإنه
يحطّ الليبل جوّه `poweredRow` **جنب** العلامة. ده **صح تمامًا للبنية اللي
اتكتب عليها** — بلوك الهوية بتلات صفوف. بس الفرع ده **كان بدّل البلوك ده كله**
بصف واحد `brandLockup`، فمفيش سطر منفصل باقي عشان الإصلاح يشيله — الليبل جوّه
الصف أصلاً في الحالتين.

**اللي اتحافظ عليه هو العمود مش الجنب-لجنب، والسبب أفقي:**
جنب بعض الليبل والعلامة بياخدوا **~110dp من صف 358dp** و**«PROPERTIES» بتتقصّ
لـ«PRO…»** — ضرر هوية، وده الشيء الوحيد اللي البند ده ممنوع يعمله. مركّبين فوق
بعض بياخدوا **~58dp** و**مبيزوّدوش ارتفاع خالص**: ليبل 8pt + لوجو 14dp = ~24dp،
تحت الـ32dp بتاع علامة B اللي بتحدد ارتفاع الصف أصلاً.

**الشكلين الاتنين بيرضّوا شكوى المالك.** ده بيرضّيها **من غير ما يدفع العرض**.
التعليق في الملف مسجّل **التاريخين** ومكتوب فيه التبديل بالظبط — **لو عايز الشكل
الـinline هنا كمان، تعديلين وخلاص. قرارك، وأنا مش هزعل.**

### ثانياً — التكليف: «حارسك يتأكد إن أي مكوّن حسّاس ليه اختبار رندر»

#### 🔴 لقيت نص الوعد مكشوف

`PresenceDot.tsx` بيصدّر **مكوّنين مش واحد**: `PresenceDot` و`PresenceLabel`،
و**الاتنين بيقطعوا نفس الوعد** — `presence-privacy-guard.test.mjs` بيعدّ
**اتنين** allow-list عشان بالظبط السبب ده: «both the dot and the label must
refuse anything outside online/recently».

**طبقة الرندر نزلت مثبتة الوعد للنقطة بس. الليبل مكانش عليه ولا اختبار.**

والليبل هو **النص الأخطر مش الأأمن**:
- بيرسم **نص**، والنص هو المكان اللي التسريب فيه **مقروء**. نقطة بتفرق ببكسل
  حدود حاجة خفية؛ ليبل بيقول كلمة لـ`away` وميقولش حاجة لـ`unknown` **بيقول
  للمتفرج صراحةً مين قفل الحضور**.
- بيعدّي على `useI18n`، و**`t()` بترجّع المفتاح نفسه** لما النص يكون ناقص — يعني
  ترجمة متمسوحة **مبتطلعش فاضية، بتطلع «chat.presence.away»** في هيدر المحادثة.
  ولا حارس نصي بيقرا الملف ده هيشوف حاجة غلط.
- وعكس النقطة، هو **مش** مخفي عن شجرة الوصولية — بالتصميم: النقطة زخرفة والليبل
  هو اللي بينطق الحالة. التقسيم ده بيفضل صح بس لو محدش قلبه، فاتثبّت.

**١٣ اختبار**، مركّبة جوّه `LanguageProvider` **الحقيقي** مش stub. البروڤايدر
بيقفل الأطفال ورا قراءة تخزين async (`ready ? children : null`)، فكل حالة
**بتستنى البوابة تفتح** — من غير الانتظار ده كل اختبارات «مبيرسمش حاجة» بتنجح
على الفريم الفاضي الأول، **للسبب الغلط**.

**اتحقّقت بالكسر ٣ مرات:** `away` يرسم ليبل فاضي · الليبل يخرس للـscreen reader ·
مفتاح الترجمة يبطّل يتحل. **اتنين اختبار بيقعوا في كل مرة**، والملف اترجّع بينهم.

#### الحارس — `tests/render-coverage-guard.test.mjs` · ٦ قواعد

**القاعدة اللي ليها أنياب: الاكتمال.** كل مكوّن **مُصدَّر** من ملف
render-critical لازم يكون **متركّب في مكان ما**. دي اللي مسكت `PresenceLabel`،
**وبتتعمّم**: ضيف مكوّن للملفات دي والبناء يفشل لحد ما يتغطّى — **من غير ما حد
يحتاج يلاحظ**. والباقي: المجموعة المُعلَنة تفضل ليها حارسها النصي · أي ملف في
`tests/render` **لازم ينادي `render()`** و**ممنوع يقرا مصدر** · مفيش اختبار
رندر بره المجموعة · `test:render` **في السلسلة** · و`testMatch` لسه شايف
`tests/render` ولسه مستثني حراس الـ`.mjs`.

**والمجموعة قايمة مُعلَنة مش كاشف أوتوماتيكي، والملف كاتب ليه:** أول نسخة حاولت
تستنتجها بمسح مفردات الادعاء في الحراس، **فمسكت `production-wiring-guard` على
سطر بيتكلم عن `maintainVisibleContentPosition`**. كاشف بيبلّغ مجموعة غلط بيقرا
أخضر وهو مش مثبت حاجة — **نفس العيب اللي الطبقة دي اتعملت تشيله، راجع درجة فوق.**

**كل قاعدة من الستة اتحقّقت بإرجاع العيب بتاعها:** امسح سويتة الليبل · ضيف
تصدير تالت · خلي اختبار رندر ينادي `readFileSync` · شيل `test:render` من
السلسلة · وجّه `testMatch` على لا حاجة. **كل واحدة بتفشل قاعدتها هي بس.**

#### وعلامتين في سلسلة النزاهة (198 → **200**)

طبقة جديدة = **أسهل حاجة تتفقد**. سكربت واحد يتشال من السلسلة و**٢٥ ادعاء
تركيب يبطّلوا يشتغلوا** والحراس النصية لسه بتطبع حيطة خضرا. العلامتين:
السويتة وحارسها **يفضلوا في السلسلة** · والـ**renderer-ين** يفضلوا على
allow-list. **الاتنين اتحقّقوا بالفشل.**

### البوابات — على الحالة المدموجة، بالأمر والنتيجة

```
pnpm run typecheck                      → exit 0
pnpm test  (الحراس النصية)              → 357/357   (كانت 351)
pnpm test:render  (jest، مربوط بالسلسلة) → 25/25     (كانت 12)
node scripts/chain-integrity-gate.mjs    → 200/200   (كانت 198)
```

### ⚠️ اللي مقدرتش أتحقق منه

1. **الـ~110dp والـ~58dp حسبة مش قياس.** مفيش محرّك layout في اختبار الرندر —
   `react-test-renderer` بيدّي الشجرة مش الأبعاد. الأرقام من الستايلات
   (58dp لوجو + عرض النص عند 8pt/1.1 تباعد). **«PROPERTIES» بتتقصّ ولا لأ في
   الشكل الـinline محتاجة جهاز حقيقي** — لو عندك لقطة للعقارات من الشكل الجديد
   دي تحسم الموضوع في ثانية.
2. **مشوفتش الشاشة.** التطبيق هنا بيفتح مسجّل خروج بصفر نداء API. الرندر
   حقيقي والادعاءات متحقّقة، **بس الحكم البصري النهائي لسه مش عندي.**
3. **`PresenceLabel` اتحقّق منه على `jest-expo/ios` بس** — نفس اختيار main
   المتعمّد (المكوّن مفيهوش فروع native). لو فيه فرق أندرويد في رسم النص، الطبقة
   دي مش هتشوفه.

---

### [اكتب رسالتك الجديدة هنا]

```
التاريخ:
الوكيل:
الـ SHA:
اتنفذ:
البوابات:
وقفت عند:
السؤال:
```

---

## المستندات المرجعية

| # | الملف | ليه |
|---|---|---|
| 1 | `MASTER-PROFESSIONAL-HANDOVER-OWNER-TO-CLAUDE-AR.md` | **الفلسفة والبروتوكول والتشريح الجراحي** |
| 2 | `MASTER-HANDOFF-PRODUCTION-MANAGER-FULL-AR.md` | التاريخ وأماكن التعديل بالسطر |
| 3 | `TO-CLAUDE-AGENT-ON-REPO-HEADERS-HANDOVER-AR.md` | التسليم التنفيذي |
| 4 | `DYNAMIC-HEADER-SPEC-AR.md` | مواصفة المالك + التعارضات |
| 5 | `MINIAPP-PER-SECTION-DEEP-STUDY-AR.md` | دراسة الزميل العميقة |
