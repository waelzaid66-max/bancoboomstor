# الجرد العميق — الخرايط · الماسنجر · حالة البناء

**التاريخ:** 2026-08-03 · **من:** المالك · **الأساس:** `main` = `ecdf776`

> كل رقم تحته أمر. واللي مقدرتش أتحقق منه مكتوب إنه كده — **مش مكتوب إنه تمام**.

---

# 1) مين كان شغال على إيه — بالأرقام

كل الوكلاء بيكوميتوا باسم `Claude <noreply@anthropic.com>`. التمييز الحقيقي في
تريلر `Claude-Session`:

```bash
git log --format='%B' | grep -oE 'session_[A-Za-z0-9]+' | sort | uniq -c | sort -rn
```

| الجلسة | كوميتات | اشتغل على |
|---|---|---|
| `…01E5sJSQe5…` | **52** | الهيدرز · الحجز · **الماسنجر والحضور بالكامل** |
| `…018PmUduRx…` | **10** | «الوكيل B» — أحمر الاستيراد · الهوية الصوتية · طي هيدر السيارات · علامة B-OOM |
| `…011AM6Fmw…` | **3** | أنا (دور المالك) |
| `…01Kf292QXo…` | **1** | مدقّق مستقل |
| `Banco Group` | **17** | إنت |

**الإجمالي 84 كوميت.**

---

# 2) 🔴 الخرايط — **محدش اشتغل عليها في الريبو ده. ولا كوميت واحد.**

```bash
git log --oneline -- 'artifacts/banco-mobile/components/search/SearchResultsMap*' \
  'artifacts/banco-mobile/components/search/mapHtml.ts' \
  'artifacts/banco-mobile/components/search/maps/' \
  'lib/search-contract/src/map.ts' 'artifacts/banco-web/components/SearchMap*'
```

**النتيجة: كوميت واحد يتيم** — `89d28d3` «chore: initialise bancoboomstor».

الكوميت ده **2536 ملف · 402,921 سطر** بتاريخ 08-01 04:23. يعني الريبو ده أصلًا
**نسخة مستوردة دفعة واحدة** من الريبو المصدر.

**وشغل الصيانة اللي إنت فاكره (MAP-01 لحد MAP-10) اتعمل على فرع اسمه:**
```
cursor/production-wiring-messenger-maps-1e3d
```
**والفرع ده مش موجود هنا:**
```bash
git ls-remote --heads origin | grep -i cursor    # صفر
```

## يعني إيه بالظبط

الخرايط **شغالة وموصولة** — بس هي **كود موروث**، مش شغل جاري. الزميل اللي عمل
صيانتها خلّص في مكان تاني، والنتيجة وصلت هنا كسنابشوت. **مفيش حد «يكمّل» —
مفيش شغل ناقص مفتوح على مكتبه.**

## والوصلات متحققة فعلًا

| الطبقة | الحالة |
|---|---|
| الخادم `GET /v1/search/map` | ✅ `routes/v1/search.ts:16` → `mapClustersHandler` |
| الموبايل بينده عليها | ✅ `SearchResultsMap.tsx` |
| Leaflet | ✅ **مضمّن جوه الريبو** (756 سطر) — بيشتغل والـCDN مقفول |

## 🔴 لكن فيه ثلاث فجوات حقيقية

### ف-١ · الويب والموبايل على **مكدسين مختلفين**

- **الويب:** Google Maps (`SearchGoogleMap.tsx`)
- **الموبايل:** Leaflet + OSM

نفس الميزة متبنية مرتين بتقنيتين. أي فلتر يتضاف لازم يتعمل مرتين.

### ف-٢ · 🔴 **خريطة الويب بتسقط لصورة ساكتة — من غير ما تحذّر حد**

> **⚠️ تصحيح ذاتي (نفس اليوم):** أول نسخة من المستند ده كتبت إن المفتاح «مش
> موثّق خالص». **ده كان غلط.** المفتاح موجود في:
> `artifacts/banco-website/.env.example` · `.env.staging.example` ·
> `deploy/aws/env/.env.banco-web.{production,staging}.example`.
> غايب من `.env.example` **الجذر** بس — ومش ده المكان اللي ناشر الموقع بيبص
> فيه. **الفجوة الحقيقية مش التوثيق — هي الصمت.** والباقي تحت صحيح.

```ts
// SearchMapSurface.tsx:42
const hasGoogleKey = searchConfig.map.googleMapsApiKey.length > 0;
if (hasGoogleKey && onViewportChange) { return <SearchGoogleMap .../>; }
return <SearchMapClusterCanvas .../>;      // ← كانفس ساكن، مفيش تحريك ولا تكبير
```

والمفتاح:
```ts
googleMapsApiKey: process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY ?? ""
```

**والسيناريو الحقيقي محدد جدًا** — الخريطة أصلًا خلف علم:
```ts
enabled: process.env.NEXT_PUBLIC_WEB_SEARCH_MAP === "true"   // افتراضه false
```

فالمشكلة مش «حد نسي المفتاح»، هي: **حد شغّل الخريطة عن قصد ونسي المفتاح**.

**والسقوط صامت بالكامل — متحقق منه بالأمر:**
```bash
grep -rn "console\.\|warn" components/SearchMapSurface.tsx lib/search-config.ts
# ❌ صفر تحذير
grep -c "onClick\|onPointer\|onWheel\|onDrag" components/SearchMapClusterCanvas.tsx
# 0 ← ولا معالج تفاعل واحد
```

يعني: **صفر لوج للمشغّل · صفر تسمية للمستخدم · صفر تحريك أو تكبير**. خريطة
تبان منشورة وبتشتغل لحد. **دي أسوأ نوع فجوة: بتفشل بهدوء وتبان شغالة.**

**✅ اتصلح:** تحذير مرة واحدة لكل تحميل صفحة، **بيولّع على الاقتران ده بالظبط**
(الخريطة مفعّلة + المفتاح فاضي) — مش على أي مفتاح ناقص، عشان التحذير ما يبقاش
ضوضاء يتعوّد الناس يتجاهلوها.

### ف-٣ · التايلات لسه بتحتاج شبكة

```
mapHtml.ts:68 → https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
```
Leaflet نفسه مضمّن، بس الصور لأ. من غير شبكة لـOSM → خريطة فاضية. **ده موثّق
كقرار تصميم (MAP-07b)، مش عيب.**

---

# 3) الماسنجر — الزميل ده لسه شغال، وشغله أحدث حاجة في الريبو

**الجلسة `…01E5sJSQe5…`** عملت الموجة كلها في يوم واحد (08-03):

| الوقت | البند |
|---|---|
| 11:34 | تسمية المساعد B · تهدئة فقاعة المحادثة |
| 11:40 | زر الإرسال كان مختفي وهو ساكن |
| 11:51 | ردود سريعة حسب القسم |
| 11:56 | أفاتار بحرف بدل دايرة فاضية |
| 12:07→12:22 | **الحضور: قاعدة بيانات → خادم → عقد → صندوق الوارد** |
| 18:32 | الحضور جوه المحادثة نفسها |

**وقبلها:** `5e389b3` (08-01) — وقف استطلاع المحادثات من كل شاشة في التطبيق.

## الحالة التقنية

| البند | الحقيقة |
|---|---|
| المعمار | **استطلاع بس** — مفيش WebSocket |
| المحادثة | `refetchInterval: 3000` |
| الوارد | `refetchInterval: 8000` (لما تكون ظاهرة) |
| الميجريشن | ✅ `0002` = `last_seen_at` · `0003` = `show_presence` — مسجّلين صح |

**نتيجة مباشرة:** الحضور ماشي على نفس الاستطلاع، يعني **«متصل» في الوارد ممكن
تكون قديمة لحد 8 ثواني**. ودي مش باج — ده نتيجة اختيار المعمار.

## الناقص

| ID | البند | ليه واقف |
|---|---|---|
| MSG-08b | حظر متبادل صريح | محتاج schema |
| MSG-13 | كتم لكل محادثة | محتاج schema |
| MSG-14c | تسجيل صوتي | الـAPI بيقبل صوت · الواجهة لأ |
| — | **الحضور بصريًا** | 🔴 **محدش شافه** — Clerk بيرفض أصل البيئة دي |

---

# 4) 🔴 حالة البناء — وهنا أوحش حاجة في الجرد

## البناء: **4 حزم من 8**

```yaml
# .github/workflows/ci.yml
--filter @workspace/api-server --filter @workspace/dealer-os
--filter @workspace/admin-os  --filter @workspace/landing
```

| الحزمة | بتتبنى في CI؟ |
|---|---|
| api-server · dealer-os · admin-os · landing | ✅ |
| **banco-mobile** | 🔴 **لأ — ولا مرة** |
| **banco-website** | 🔴 **لأ** (في `ci-website.yml` المفلتر بالمسارات، واللي معملش ران أبدًا) |
| banco-web | مجمّد رسميًا (`FROZEN.md`) |
| mockup-sandbox | لأ |

**يعني: التطبيق اللي المستخدم بيستعمله، وموقع المستهلك — مفيش أي تحقق من إنهم
بيتبنوا أصلًا.**

## 🔴 الاختبارات — الرقم بيقول حاجة، والحقيقة حاجة تانية

| الطبقة | العدد | النوع |
|---|---|---|
| **api-server** | **80 ملف اختبار** | ✅ **تكامل حقيقي على Postgres فعلي** — طلع أخضر في CI |
| **banco-mobile** | 313 «اختبار» | 🔴 **كلها تحليل نصي للمصدر** |

```bash
grep -h "^import" artifacts/banco-mobile/tests/*.mjs | grep -v "node:"
# مفيش ولا رندرر واحد
```

**اختبارات الموبايل بتقرا الملفات كنص وتدوّر بـregex.** مفيش رندر لمكوّن، مفيش
تفاعل، مفيش وقت تشغيل.

**النتيجة الصريحة:** ممكن شاشة تكون **مكسورة تمامًا وقت التشغيل** والـ313 كلهم
يعدّوا. الرقم بيقيس «النص متطابق مع النمط المتوقع» — **مش «التطبيق شغال»**.

**ودي أكبر فجوة صدق في الريبو كله.** كل مستند بيقول «313 نجحوا» بيدّي انطباع
مالوش أساس.

## الـCI

| الحقيقة | القيمة |
|---|---|
| تشغيلات قبل النهاردة | **صفر** |
| تشغيلات النهاردة | 5 — كلها أنا |
| `ci-website.yml` | 🔴 **معملش ران ولا مرة** |
| التولّع التلقائي على كوميتات الوكلاء | 🔴 **مابيحصلش** — لا `push` ولا `pull_request` |

---

# 5) حاجة **فحصتها وطلعت سليمة** — عشان الجرد يبقى متوازن

في رسالة `032bf00` مكتوب إن ميجريشن `users.language` **لسه ما اتعملش**
(«drizzle-kit generate علّق»). **ده اتقفل بعدها:**

```bash
grep -rn "language" lib/db/migrations/*.sql
# 0001_minor_stingray.sql:1  ALTER TABLE "users" ADD COLUMN "language" text;
```

سجل الميجريشن `0000→0003` متسلسل وسليم. **مفيش انحراف بين الـschema والميجريشن.**

---

# 6) الخلاصة بصدق

## اللي متحقق منه فعلًا ✅
- الخادم: 80 ملف اختبار تكامل على Postgres حقيقي — **أخضر في CI**
- الميجريشن: 0000→0003 سليمة ومتسلسلة
- الخرايط موصولة من الخادم للموبايل
- سلسلة النزاهة 198/198

## المشاكل الحقيقية 🔴
1. **موبايل وموقع المستهلك مالهمش أي تحقق بناء** — 4 من 8 حزم بس
2. **313 اختبار موبايل كلهم نص — صفر وقت تشغيل**
3. **خريطة الويب بتسقط لصورة ساكتة من غير تحذير**، والمفتاح مش موثّق
4. **الـCI مابيولّعش لوحده** — أي «أخضر» من غير `workflow_dispatch` كلام فاضي
5. الخرايط متبنية مرتين بتقنيتين مختلفتين

## الناقص المعروف والمقبول 🟡
MSG-08b · MSG-13 · MSG-14c · MAP-08b · MAP-07b · تصديق تسليم الإشعارات (NOTIF-02/10 — بيانات اعتماد خارجية)

## اللي محدش يقدر يقول عنه حاجة ⬜
**كل حكم بصري.** الحضور · الهيدرز · أي شاشة. Clerk بيرفض أصل البيئات دي،
**فالقياس الهندسي موثوق والحكم البصري لأ.**

---

# 7) توصيتي كأولوية

| # | البند | ليه |
|---|---|---|
| 1 | **ضُم `banco-mobile` و`banco-website` لبناء CI** | حزمتين للمستخدم النهائي بلا أي تحقق |
| 2 | **`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` في `.env.example`** + تحذير لما يبقى فاضي | فشل صامت في الإنتاج |
| 3 | **أول اختبار رندر حقيقي للموبايل** | يكسر وهم الـ313 |
| 4 | تواريخ الحجز في العقد (B1) | قسم حجز مبيعرفش تاريخ |

— المالك
