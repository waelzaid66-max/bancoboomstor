# سجل التحقق — الجولة الثانية

**الدور:** مهندس تحقق · **المرجع:** `HEAD = 96c8ed4` · `origin/main = d3f8df1`
**وقت التحقق:** 2026-08-06 · الشجرة نضيفة

> بحث عن مشكلات **جديدة**. النتيجة: **مشكلتان حقيقيتان · خمسة إنذارات كاذبة
> أُسقطت قبل رفعها**.

---

# 🔴 ت-١٢ · الـAPI يقدر يقلع على قاعدة غير مُهاجَرة

| | |
|---|---|
| **Claim** | مفيش ضمان ترتيبي إن الميجريشن تتنفّذ قبل الـAPI |
| **Evidence** | `docker-compose.coolify.yml:85` → `migrate: profiles: ["migrate"]` — **مابتشتغلش افتراضيًا**<br>خدمة `api` → `depends_on: postgres: condition: service_healthy` — **`migrate` غير مذكورة** |
| **Commit SHA** | — (حالة ملف على HEAD) |
| **Files** | `docker-compose.coolify.yml` |
| **Verification** | ✅ **مثبت على HEAD** |
| **Impact** | لو المشغّل نسي `--profile migrate run --rm migrate`، الـAPI بيقلع على schema قديم. **مفيش حاجة توقفه** |
| **Recommendation** | قرار المدير: إما `depends_on: migrate: condition: service_completed_successfully`، أو فحص إقلاع يفشل لو الميجريشن ناقصة. **مش قرار وكيل** |

**ملاحظة متوازنة:** الأمر **موثّق بالنص** في `COOLIFY_DEPLOY_NOW.md:130` و
`docs/DEPLOY_COOLIFY.md:219,388`. **الخطر تشغيلي مش توثيقي.**

---

# 🔴 ت-١٣ · ميزة البريد ثنائي اللغة **مكتملة على الخادم وغير قابلة للوصول من التطبيق**

| | |
|---|---|
| **Claim** | تفضيل اللغة لا يصل للخادم أبدًا |
| **Evidence** | `context/LanguageContext.tsx:88-92`:<br>`setLang` بيعمل `setLangState` + `AsyncStorage.setItem` **وبس**<br>`:92` → `// TODO(lang-sync): also PATCH /v1/me with { language: l }` |
| **Files** | `artifacts/banco-mobile/context/LanguageContext.tsx` |
| **Verification** | ✅ **مثبت** |

## السلسلة — متحقق منها طرفًا بطرف

| الطبقة | الحالة |
|---|---|
| `users.language` (عمود + ميجريشن `0001`) | ✅ موجود |
| `UpdateMeSchema` بيقبل `language` | ✅ |
| `updateUserProfile` بيحفظه | ✅ (وأنا صلّحت نوعه في `65ab6d0`) |
| `EmailService` بيحلّ لغة المستلم منه | ✅ |
| **التطبيق يبعته** | 🔴 **لأ** |

| | |
|---|---|
| **Impact** | **كل بريد في كل الأسواق يخرج بالعربية** — تمامًا كما قبل بناء الميزة. القوالب الإنجليزية مبنية بالكامل ولا تصل لأحد |
| **العائق الموثّق** | `language` لازم يتضاف لجسم `PATCH /v1/me` في `openapi.yaml` ثم `orval` يُعاد تشغيله |
| **Recommendation** | بند قابل للتنفيذ فورًا · **يُسند لمن يملك العقد (B1)** |

## ملاحظة تخصني

إصلاحي في `65ab6d0` خلّى `updateUserProfile` **يتكمبل**. **مخلّاش الميزة
قابلة للوصول.** الاتنين مختلفين، والتقرير السابق ما فرّقش بينهم.

---

# ✅ نتائج سلبية — فحصتها ولم أجد مشكلة

**أذكرها لأن الإنذار الكاذب أخطر من الصمت.**

## ن-١ · صفر حارس غير مربوط
```bash
for f in tests/*.test.mjs; do grep -q "$(basename $f)" package.json || echo "غير مربوط: $f"; done
# صفر من 24
```
**النمط اللي حصل قبل كده (`car-hero-honesty-guard`) مقفول حاليًا.**

## ن-٢ · صفر أسرار حقيقية — ثلاث مطابقات فحصتها وأسقطتها

| الملف | المطابقة | الحقيقة |
|---|---|---|
| `PaymentConfigService.test.ts:165,167` | `egy_pk_live_PUBLIC` · `egy_sk_live_SECRET` | **بيانات اختبار** — و`:186` بيؤكد إن السر **لا يُتسلسل** (اختبار أمني) |
| `useSocialProviders.ts:45` | `pk_live_<base64(...)>` | **تعليق** يشرح صيغة مفتاح Clerk |
| `Dockerfile.banco-web:9` | `pk_live_...` | **تعليق** يوضح الاستخدام |

**ولا ملف `.env` متتبّع في git.**

## ن-٣ · قيد تكافؤ i18n قائم
`constants/i18n.ts:2380` → `const ar: typeof en = {` — **مفتاح ناقص يفشل
الكمبايلر**، مش وقت التشغيل.

## ن-٤ · كثافة الديون منخفضة
```bash
git grep -cE "TODO|FIXME|HACK|XXX" -- 'artifacts/**/*.ts*' ':!*test*'
# 2 في ملفين فقط
```
واحد منهما هو ت-١٣ أعلاه. **الملف الآخر تعليق شرح، ليس دَينًا.**

## ن-٥ · تصحيح لصياغتي عن `banco-web`
`docker-compose.coolify.yml:190` → `banco-web: profiles: ["legacy-banco-web"]`
**الحزمة المجمّدة لا تعمل افتراضيًا على Coolify كذلك.** صياغتي السابقة
أوحت بأنها تُنشر — **غير دقيق**.

---

# ملخّص الجولة الثانية

| الحالة | العدد | البنود |
|---|---|---|
| 🔴 مشكلة جديدة مثبتة | **2** | ت-١٢ · ت-١٣ |
| ✅ فُحص ولا مشكلة | **5** | ن-١ … ن-٥ |
| ❌ إنذار كاذب أُسقط قبل الرفع | **3** | مطابقات الأسرار |
| ⚠️ تصحيح صياغة لي | **1** | ن-٥ |

---

# الحصيلة عبر الجولتين

| # | البند | الحالة | من يقرر |
|---|---|---|---|
| ت-١ | `send` ناقصة من `FILLED` على `main` | ✅ مثبت | دمج فوري — صفر تعارض |
| ت-٢ | صفر أوسمة · النشر معطّل | ✅ مثبت | **المالك** |
| ت-٣ | ~~AWS ينشر المجمّد~~ | ❌ **سقط** | — |
| ت-٤ | تواريخ الحجز = صفر | ✅ مثبت | B1 |
| ت-٥ | `Dockerfile.banco-web` ميت | ✅ مثبت | المدير |
| ت-٦ | 200 اسم أيقونة فخ · أثر حالي صفر | ✅ مثبت | المدير |
| ت-٧ | `sectionAccentAlpha` مكررة | ✅ بشرط الدمج | تدخل يدوي |
| ت-٨ | الهيدرز مقسومة — 569 سطر | ✅ مثبت | المدير |
| ت-٩ | إصلاحي لـPOWERED BY ناقص | ⚠️ جزئي | المدير |
| ت-١٠ | 318 تأكيد · 24 ملف · 80 خادم | ✅ بالتشغيل | — |
| ت-١١ | 3 مستندات تشير لريبو غلط | ✅ مثبت | تصحيح نصي |
| **ت-١٢** | **API يقلع على schema غير مُهاجَر** | ✅ مثبت | **المدير** |
| **ت-١٣** | **البريد الإنجليزي لا يصل لأحد** | ✅ مثبت | **B1** |

**إجمالي: 11 مثبت · 1 جزئي · 1 ساقط.**

---

# ما لا أستطيع إثباته

| البند | السبب |
|---|---|
| أي حكم بصري | Clerk يرفض أصل هذه البيئة |
| قياس 110dp/58dp في تعليق الفرع | **غير قابل للإثبات بالأدلة الحالية** |
| أن ت-١٢ وقع فعليًا في نشر حقيقي | **غير قابل للإثبات** — أفحص التهيئة، لا سجلات الإنتاج |

---

**لم أعدّل أي كود. لم أتخذ أي قرار معماري.**

— مهندس التحقق
